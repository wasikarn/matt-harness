{
  "$schema": "https://json.schemastore.org/claude-code-hooks.json",
  "hooks": {
    "SessionStart": [
      {
        "id": "session:doctrine-bootstrap",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session/doctrine-bootstrap.sh\""
          }
        ],
        "description": "Inject METHODOLOGY.md doctrine (decision-sizing triad + reasoning scaffold) into session context"
      },
      {
        "id": "session:command-root-anchor",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session/command-root-anchor.sh\""
          }
        ],
        "description": "Bridge hook-only ${CLAUDE_PLUGIN_ROOT} into the session as ${KBG_PLUGIN_ROOT} so command markdown (no official ${CLAUDE_COMMAND_DIR}) can reference bundled scripts portably"
      }
    ],
    "UserPromptSubmit": [
      {
        "id": "advisory:flow-nudge",
        "matcher": "*",
        "description": "Advisory: when user prompt looks like non-trivial engineering work, nudge kbg:grilling → kbg:to-prd → kbg:to-issues → /ship",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/advisory/flow-nudge.sh\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "id": "gate:bash:irrecoverable",
        "matcher": "Bash",
        "description": "Gate: block irrecoverable Bash patterns (rm -rf, push --force, --no-verify, reset --hard, clean -f)",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/gates/irrecoverable.sh\""
          }
        ]
      },
      {
        "id": "gate:write:path-hardcode",
        "matcher": "Write|Edit|MultiEdit",
        "description": "Gate: block hardcoded /Users/<name> paths in .sh/.py files",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/gates/path-hardcode.sh\""
          }
        ]
      },
      {
        "id": "gate:write:verifier-protect",
        "matcher": "Write|Edit|MultiEdit",
        "description": "Gate: ask the human to approve edits to BOTH non-model verifiers — hooks/gates/** + hooks/hooks.json (deny-gates + wiring) AND skills/harness-audit/scripts/audit.sh + checks/** (audit grader). Tamper-resistance: the model cannot edit the code that judges it. Emits permissionDecision: ask (no env-var bypass).",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/gates/verifier-protect.sh\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "id": "stop:cost-tracker",
        "description": "Stop: track cumulative token and cost metrics per session; appends to ~/.local/share/kbg/metrics/costs.jsonl",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/stop/cost-tracker.sh\"",
            "async": true,
            "timeout": 10
          }
        ]
      }
    ]
  }
}