# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Validation (run before committing)

```bash
claude plugin validate . --strict
```

Plugin manifest is the primary validation gate. `scripts/run-gauntlet.sh` runs plugin-validate + full shell-lint + JSON lint + harness-audit + the hook behavioral suite (`hooks/tests/test-gates.sh` + `test-flow-nudge.sh` + `test-session-stop.sh` — deny-gate + advisory-sensor + session/stop-hook unit tests) in parallel. The broader fleet critical-hooks suite and the eval dataset gate are pending rebuild.

## Adding or removing a surface

Auto-discovered directories: `agents/`, `skills/`, `commands/`, `hooks/`, `output-styles/`, `themes/`. See `skills/add-surface/SKILL.md` for the step-by-step (manifest bump, validation, BOUNDARY.md regen).

## Git hooks

Hooks live in `git-hooks/` (not `.git/hooks/`). Wire once per clone:

```bash
git config core.hooksPath git-hooks
```

pre-commit: fast gate — syntax/lint (`bash -n` + shellcheck), JSON validation, CRITICAL harness-audit (graceful-skip if absent).
pre-push: full gauntlet (all validation layers in parallel).

## Composer-not-creator doctrine

Before writing a new skill, command, or agent from scratch, check sources in this order: **(1)**
`mattpocock/skills` first — `claude plugin list` / the Skill tool's own available-skills list for
what's already installed under the `mattpocock-skills` plugin, plus the local clone at
`/Users/kobig/Codes/Personals/mattpocock-skills` for what's upstream-available but not yet
installed. This is a **Matt-Pocock-first harness** (his skills installed as the
`mattpocock-skills@mattpocock` plugin, namespaced `mattpocock-skills:<name>` — see README.md Quick
Start; migrated off the earlier unnamespaced `gh skill` installs 2026-07-17); checking
ECC/superpowers before matt's own repo gets the priority backwards. **(2)** the upstream ECC repo
at `/Users/kobig/Codes/Personals/ECC` and
the vendored superpowers checkout at `/Users/kobig/Codes/Personals/superpowers`. **(3)** sibling
harnesses under `/Users/kobig/Codes/Personals/` worth a structural-pattern check even when they're
not kbg's primary composer sources (e.g. `oh-my-claudecode` — cherry-picked before, ask if
unsure which repos currently qualify). Cherry-pick and adapt from whichever source fits; create
kbg-native surfaces only when none do. Confirmed gap (2026-07-17): `code-implementer`/`/implement`
were built checking only (2), skipping (1) — collided with matt's own `engineering/implement`
skill, caught by the user, not by this checklist. (A hand-pinned HEAD hash is structurally doomed
to re-stale; the path is the stable anchor — run `git rev-parse HEAD` there when you need the
current commit.)

## Architecture

The plugin ships as `kbg@kobig` from the `wasikarn/kbg-harness` GitHub repo. Claude Code loads all surfaces from `~/.claude/plugins/cache/kobig/kbg/<version>/` at startup. Nothing is symlinked.

**Doctrine injection:** `hooks/session/doctrine-bootstrap.sh` fires on SessionStart and injects `docs/METHODOLOGY.md` (decision-sizing triad + reasoning scaffold) into session context via `$CLAUDE_PLUGIN_ROOT` (the plugin install dir; the older `$CLAUDE_PLUGIN_DIR` name is not a real CC variable and expands empty).

**Operating model:** deny the irrecoverable set computationally (gates in `hooks/gates/`), advise on the rest (sensors in `hooks/advisory/`). Advisory sensors never emit `permissionDecision`. The L2–L5 autonomy ladder is retired.

**Why — the unifying crux:** the gate is a *verifier* (deterministic shell returning a branchable **score**), the model is the *maker*, and the maker can never grade its own work — an LLM judging its own output is circular ("two optimists agreeing"). So advisory sensors journal but never gate, and the autonomy ladder had to retire: a model-as-gate is the maker appointing its own verifier. **Score, not feel** — every loop's stop condition must be a number a deterministic gate can branch on, never a vibe the model rationalizes. (This is the agent-loop verifier-separation principle; see `docs/research/` + the retired L2–L5 build for the proven failure it prevents.)

When hooks are wired: gates/ (deny), advisory/ (journal), session/ (inject), stop/ (cost tracking).

## Skill authoring doctrine (matt-pocock)

When creating or editing a skill under `skills/`, follow matt-pocock's `writing-great-skills` doctrine — canonical: the `mattpocock-skills:writing-great-skills` skill (installed as the `mattpocock-skills` plugin, not vendored in this repo since v0.46.0 — see README.md Quick Start; leading word, ≤25-word description, completion criterion, no-op test, two-cuts, failure-mode guard).

The `docs/skill-template/SKILL.md` template carries this checklist as a `## Design checks` section — but `harness-audit` check 36 does **not** check for that heading's presence. It checks 4 of the 6 doctrine elements via INFO-only regex proxies against each skill's live description/body (leading-word vocabulary, ≤25-word count, completion-criterion phrasing, a no-op-test line-count heuristic); "two-cuts" and "failure-mode guard" have no shell check — a failure-mode regex proxy was tried and retired 2026-07-16 (vacuous before a reset-bug fix, 5/5 false-positive after: every flagged skill already named its failure mode in a prose section or bullet list the numbered-window proxy couldn't see). INFO findings never fail the gate. Confirmed 2026-07-22: only 2/35 native skills (`pr`, `task-prep`) actually carry a `## Design checks` section — the template's checklist is documentation, not an enforced requirement.

**Named Model footers:** a skill/command/agent that makes load-bearing reasoning/judgment choices may end with a `## Named Model` footer citing cc-thinking-skills lenses. Apply the 3-condition rubric from `memory/mental-models-sweep-v0302-2026-07-03.md`: (1) load-bearing reasoning gap, (2) name-a-lens benefit for the operator, (3) honesty posture preserved (footer is a scaffold + catalog pointer, never "this lens proves correctness"). The curated catalog is `docs/reference/reasoning-models.md`; the 39 raw models live under `docs/reference/thinking-skills/skills/`.

**Suggested next step footers:** a workflow surface (command or workflow skill run as a discrete step) may end its Output/Summary phase with a `Suggested next step:` marker — outcome-branched (`situation → action`), citing skills as `kbg:<name>` and commands as `/<name>`. Skills are ALWAYS cited `kbg:`-form (never `/name`) — get this right at authoring time: `harness-audit` check 40 only catches rename/deletion drift on refs already in `kbg:` form, it does **not** scan for a skill mis-cited in slash form (confirmed: this exact bug shipped twice — `commands/pr.md` and `diagnosing-bugs/SKILL.md` both cited a skill as `/name` undetected until a manual survey caught it, v0.35.0). Passive suggestion only — never "invoke X now" / auto-chain (that collides with the no-model-self-start doctrine). Skip self-contained reference/pattern/catalog surfaces (a forced footer there is the retired canonical-sections ceremony, 2026-06-16) and terminal workflows (post-mortem, ship-release terminus).

**Escalation to `AskUserQuestion`:** a branch belongs in the passive footer only while it's anticipatory — conditional on a fact not yet known (did the reviewer comment, did CI go red). If every branch is already true/decidable right now and there's no sensible default, that's a present-tense fork, not a suggestion — surface it via `AskUserQuestion` (per `output-styles/staff-eng.md`'s decision-question rule: one-line consequence per option) instead of text the user might not read. Model: obra/superpowers' `finishing-a-development-branch` skill, which ends by presenting exactly N concrete options (merge/PR/keep/discard) and blocking for the pick — not superpowers' separate (and rejected) `using-superpowers` auto-chain directive. None of kbg's shipped footers (v0.35.0/.1) currently qualify — they're all anticipatory-conditional — so this is a criterion for future surfaces, not a rewrite of what shipped.

## Branching model

Single branch: `develop` only. No feature branches. Commit and push direct.

**Computationally enforced** by `gate:worktree:develop-only` (`WorktreeCreate` event) and the `git worktree add -b` block in `gate:bash:irrecoverable` (`PreToolUse:Bash`). Both gates are opt-in per repo via the `/.kbg-no-worktree` sentinel — present in the kbg-harness repo, absent from tathep/ECC/scratch repos (which keep their existing `gate:write:worktree-guard` redirect). Detached `review-pr-<N>` worktrees in `$TMPDIR` are explicitly allowlisted so the Phase 2 PR-by-number review path keeps working.

## Non-obvious gotchas

Grouped by behavior area (not a flat bucket — find the group first, then the line).

### Repo & commit hygiene

- **Hardcoded home paths blocked:** `.sh`/`.py` files must use `$HOME` or `~`, never `/Users/<name>`. The pre-commit gate will reject the commit.
- **Never `rm -rf`:** use `trash` for deletions.
- **Never `--no-verify`** on commits or pushes.
- **Stage by name:** never `git add -A` or `git add .`.

### Plugin lifecycle & install

- **`defaultEnabled: false`:** plugin ships disabled. After install, add `"kbg@kobig": true` to Claude Code `settings.json`, then restart.
- **Cache-invalidation:** same-version edits are no-ops. Always bump both manifests before `claude plugin update`. CLAUDE.md-only edits skip the bump — it's dev-facing repo guidance, not cached plugin content (only `agents/`/`skills/`/`commands/`/`hooks/`/`output-styles/`/`themes/` ship per-version).
- **`BOUNDARY.md` regen:** the script writes to STDOUT, not the file. The `> BOUNDARY.md` redirect is required every time.
- **Output style:** `output-styles/staff-eng.md` is the sole live-response register — self-calibrates terse vs full decision-framing by stakes, not by switching files (the old `senior-eng.md`/`staff-eng.md` two-file split was collapsed 2026-07-02; the internal "Calibrate to stakes" rule replaces the escalation/fallback dance). `force-for-plugin: true` auto-activates it whenever `kbg@kobig` is enabled, overriding the user's own `outputStyle` setting — no `/output-style` selection needed, but it also means you can't run a different style while this plugin is on without disabling it first.

### Session environment quirks

- **Working frames:** `contexts/` holds `dev.md`, `review.md`, `research.md` — loaded by `/frame` to set session posture.
- **`grep` is aliased** to `rtk grep` in this environment. Use `/usr/bin/grep` or `awk` for count/stat operations.

### Skill/agent/command mechanics & routing

- **Skill descriptions load on every Task spawn** (~words×1.3 tokens). Keep descriptions ≤25 words.
- **Thinking models:** default is the triad + `advisor()` inline (METHODOLOGY Rule 1) — `kbg:decide` is on-demand only, for genuinely hard/contested-diagnosis choices (de-scoped 2026-07-02, v0.21.4: 0 real-world invocations vs 55 `advisor()` calls across 182 sessions). The 39 on-demand mental-model files live in `docs/reference/thinking-skills/skills/` (never move to `skills/` — would break fleet count).
- **`disable-model-invocation: true`:** carried by 2 skills currently — `recursive-improve` and `score-decision` (re-check via `grep -rl "disable-model-invocation: true" skills/*/SKILL.md`; the count drifted hard after v0.46.0 moved 17 flag-carrying matt-origin skills out of this repo, and nobody updated this line). Both are now **CRIT**-guarded against the flag being silently dropped: `recursive-improve/SKILL.md` by check 39, `score-decision/SKILL.md` by check 49 (added 2026-07-23 after a `kbg:plan-reviewer` pass on a skill-improvement batch plan flagged that skill-creator's own description-optimizer rewrites SKILL.md frontmatter, and only recursive-improve had a real gate). Check 30 still only WARNs that a `-reason` field exists on whichever skills currently carry the flag — it's the presence-of-reason check, not the flag-survives-a-rewrite check; the two CRIT checks above are what actually close that gap now.
- **`review_mode` in `ship-merge`:** `review-pr` tags its state write `pr-by-number` (isolated worktree) or `own-branch` (self-review). `ship-merge` caps the Critical-findings score at the fatal-weakness floor on sensitive-path diffs reviewed `own-branch` — an automation-bias guard against trusting a same-session self-review's severity tiering. "Sensitive-path" covers both auth/secret/credential/payment/billing/token AND the harness's own verifier/gate code (`hooks/gates/**`, `hooks/hooks.json`, `skills/harness-audit/scripts/{audit.sh,checks/**}` — the same list `hooks/gates/verifier-protect.sh` protects).
- **`orchestrate` vs `kbg:decide`:** orchestrate decides whether/how to spend effort on an ask (inline/parallel/sequential/drop, which surface receives it) *before* it's understood as a bounded decision; `decide` reasons through a bounded question once you're already committed to answering it. A pile of competing asks routes through `orchestrate` first; a single reversible-choice question goes straight to `decide`.
- **`SKILL.md` frontmatter ≠ `agents/*.md` frontmatter:** the real skill-file field for tool control is `allowed-tools` (pre-approves without asking; there's no hard-restriction field for skills), not `tools:` — that's the `agents/*.md` subagent field (hard-restricts to a fixed set). Confirmed against the official 16-field `skills.md` reference and the shipped CLI binary's own compiled schema key list. `metadata` / `metadata.origin` / `disable-model-invocation-reason` are non-standard-but-harmless kbg conventions — Claude Code tolerates unrecognized frontmatter keys (confirmed via changelog + `plugin.json`'s own documented "unrecognized fields → warning, not error" policy), they just carry zero behavioral effect.
- **`/goal` vs `goal-craft`:** `/goal` is Claude Code's own native completion-condition loop (v2.1.139+, session-scoped, judged each turn by a separate small model that reads only the transcript). kbg never wraps or auto-invokes `/goal` itself — the user always types `/goal` themselves, no exceptions. `skills/goal-craft/SKILL.md` only composes a paste-ready condition string (mandatory one-way-door screen + turn bound) and, as of 2026-07-08, is model-invocable (its `disable-model-invocation` flag was removed on user request) — the model may draft a condition unprompted, but the string is inert until the user pastes it after `/goal`. Auto-dispatch of `/goal` itself (`claude -p "/goal ..."`) is still a deliberate non-goal — it forks a separate headless session and reopens the retired L4/L5 "no model self-start" invariant.

## Recent versions

Quick orientation for the last few releases. For full notes see `CHANGELOG.md`.

Cap this list at 10 bullets. Keep the last 3-5 releases as individual entries; fold older ones into version-range bullets (`vX.Y.a–vX.Y.b` or `vX.Y.x`) by theme. Adding an 11th bullet means dropping the oldest — `CHANGELOG.md` is the full record, this section is orientation only.

- **v0.68.43** — `skill-creator:skill-creator` improve+optimize loop run against `agents/blind-spot-hunter.md`, per user request to delegate the eval-set-review step to staff-eng agents. `advisor()` caught a structural mistake before fixture-building: copying the sibling agents' single-file fixture shape would have tested this agent on input its own "When NOT to use" section declines (its headline class is cross-file interaction) — rebuilt as 4 git-tracked multi-file mini-repos, one with its headline defect planted outside the diff entirely, every prompt asserting a fabricated prior clean `code-reviewer` pass to match the agent's real entry condition. 8 dry runs, 4 doc-first graders → 2 real fixes, both cross-fixture-confirmed: the doc conflated "not reachable now" (defer the fix) with "trace incomplete" (`unverified`) as the same situation, letting real findings disappear twice over; a raw with-agent output claimed to have "cross-checked with `advisor()`" — a tool this agent structurally cannot call, verified directly against the file, a fabricated tool-call claim from the agent whose whole job is catching exactly that failure class. 5 fresh-context whole-file re-review rounds to converge (6th confirming instance of this repo's "fix ships a new contradiction" pattern) — each round's fix left one loose thread the next round caught: an overclaimed "read-only" framing, a stale "cannot" heading, the new `unverified` value never swept through its two Output Format consumers (`CLEAN` verdict, `fix recommendation`), and an `unverified` branch that didn't cover all 3 stall causes step 5 itself names. Optimize: 2 staff-eng reviewers converged on real, non-overlapping gaps in a 19-item sequential-trigger eval set (this agent triggers on conversation state — "after code-reviewer" — not topically like most of the fleet) — zero positive coverage for data-flow-asymmetry despite it being one of only 3 hunt-target categories named in the description itself; the sequential gate's sharpest failure mode (token "code-reviewer" present, review NOT complete) was untested; no negative tested "review done, but squarely `silent-failure-hunter`'s own class." Reconciled to 22 scored + 1 moved to `manual_inspection_items`. Shipped no description change — Rule 2, only eval-set gaps found, no wording defect. Skipped live execution: per `advisor()`, a cold `claude -p` query has no prior `code-reviewer` pass in its own context, so it can't structurally reproduce this agent's real entry condition — a materially weaker signal than for a topically-triggered sibling agent.
- **v0.68.41** — `skill-creator:skill-creator` improve+optimize loop run against `agents/summarizer.md`, per user request to delegate the eval-set-review step to staff-eng agents. Improve half: 4 fixtures (vendor-incident email, on-call postmortem notes, multi-topic standup, Thai chat log), 8 dry-runs (with-agent + baseline), 4 fresh-context doc-first graders. 3 real fixes: Phase 5's structure-selection rule (prose for causal chains, bullets for parallel items, table for ≥3-item comparisons) wasn't gated into the Output Format's `summary:` field, which offered "sentences or tight bullets" as unconditional free choice — the strongest finding, live-demonstrated when the with-agent run bulleted fixture 2's causal incident chain in violation of the doc's own Anti-Pattern while the baseline (no doctrine) avoided it; Phase 7 said "empty detail section" where Output Format said "omit entirely"; Phase 6's language rule collided with Guardrail 5's stricter "match the source, not the dispatch prompt" wording. 3 fresh-context whole-file re-reviews (not diff) to converge — a 6th confirming instance of this repo's "fix ships a new contradiction" pattern: round 1 caught the Output Format rewrite's own "Per Phase 5" line dropping the Table option; round 2 caught the frontmatter `description` still claiming "prose" output despite the fix, plus a Tool-guardrails line falsely generalizing this agent's own restrictive tool grant ("no `Skill` tool available to a subagent") into a platform-wide claim; round 3 came back clean. Synced 2 stale hand-maintained mirrors of the old "prose" wording (`README.md`, `skills/orchestrate/reference.md`). Optimize half: drafted a 20-query eval set, 2 independent staff-eng reviewers — reviewer 2's finding was BLOCKING and verified: every should-trigger query described a long source ("40-page PDF") without attaching real content, 2 pointing at nonexistent file paths, which would fail every positive case for reasons unrelated to description quality once run through `detect_trigger.py` (verbatim query pass-through, no substitution). Reconciled into a final set where every should-trigger case references a real, existing repo file or inlines genuine text. Adapted `detect_trigger.py` (generalizing the target-agent-name parameter, per requirement-analyst/silent-failure-hunter precedent) and live-ran it against the installed (pre-session) description: 11/20 correct — all 10 negatives correctly non-triggering, but only 1 of 10 positives triggered, and `advisor()` caught before interpreting results that it was one of 3 cases whose query pointed at a path inside `summarizer-workspace/iteration-1/fixtures/` — this session's own test-fixture directory, named after the agent — so the one dispatch can't be attributed to description wording. Net: 0/10 positives triggered for reasons attributable to the description text, matching the confirmed non-discriminating-eval pattern (v0.68.28/.29/.30/.37). Shipped no description-triggering change — Rule 2, no demonstrated problem (the "prose"→"output" edit above is a doctrine-parity fix from the improve half, not validated by this run, since a live run measures the installed description, not the working tree).
- **v0.68.40** — `skill-creator:skill-creator` improve+optimize loop run against `agents/typescript-reviewer.md`, per user request to delegate the eval-set-review step to staff-eng agents (4th use of this substitution, after v0.68.35–.37). Improve half: an initial 4-fixture draft was rebuilt per `advisor()` around doctrine differentiators instead of generic TS knowledge — 3 real git-repo fixtures testing "When invoked" steps 1-5 and Noise Control's 6 restraint carve-outs, 3 dry-run pairs, 3 graders. 1 real fix: step 4's "if linting or TypeScript checking fails, stop and report" didn't distinguish "tool couldn't execute" from "tool ran and failed," risking a stop-cold review that silently drops a CRITICAL finding when `node_modules`/`eslint` is missing. The other 2 fixtures confirmed doctrine already works (clean stop-on-no-relevant-changes; unchanged-code CRITICAL scoping + all 6 carve-outs held) — no fix needed, stated plainly rather than manufactured. 3 fresh-context whole-file re-reviews (not diff) to converge — a 5th confirming instance of this repo's "fix ships a new contradiction" pattern (task-prep v0.68.31, decide v0.68.32, address-review v0.68.35, silent-failure-hunter v0.68.36, requirement-analyst v0.68.37): round 1 caught an off-by-one step reference plus 2 new ambiguities in the first fix; round 2's own added rationale clause then collided with Approval Criteria's severity tiers and Noise Control's diff-scoping rule. `advisor()` traced the actual root cause — both contradictions came from prescribing behavior on a branch ("check runs and finds real errors") every fixture grader had explicitly declined to resolve as untested — round 3 dropped the rationale clause instead of rewording it again, and converged clean. Round 3 also surfaced a real, pre-existing, independent gap: Approval Criteria's `Approve` ("No CRITICAL or HIGH") and `Warning` ("MEDIUM issues only") both read true on a MEDIUM-only result, no precedence rule — same defect shape already fixed once in `kbg:decide` (v0.68.32) — closed by adding "or MEDIUM" to `Approve`. Also fixed a bare (unprefixed) `backend-patterns` citation to `kbg:backend-patterns` — the exact namespace-citation gap CLAUDE.md already flags as unguarded by check 40. Optimize half: built a parameterized `detect_trigger.py` (Agent/Task-dispatch detector, `agent_name` read from the eval JSON, scoped `ids_filter` runs) reusing the requirement-analyst/silent-failure-hunter precedent. 2 independent staff-eng reviewers caught a real self-contradiction in the draft eval set (one item graded on membership semantics, three sibling items graded on the opposite "more specific competitor owns it" standard) plus a factual OWASP overclaim in that same item's justification — reconciled into a 21-item set with an explicit stated grading standard. Mid-reconciliation, self-caught and reviewer-confirmed: an initial relabel arguing "security is a core pillar, so it should trigger" was itself an overclaim — none of the file's 7 CRITICAL-Security bullets actually cover auth/broken-access-control. Deliberately skipped live execution: the set's own membership standard means no scored item can distinguish co-dispatch from exclusive dispatch on its actual boundary items, so a run would be uninformative on exactly what it exists to test — logged as a finding (`typescript-reviewer-workspace/optimize/findings.md`) naming the missing prerequisite (sequence-aware fields, per requirement-analyst's `expected_first_action` precedent) rather than run anyway.
- **v0.68.37** — `skill-creator:skill-creator` improve+optimize loop run against `agents/requirement-analyst.md`, per user request to delegate the eval-set-review step to staff-eng agents. Improve: 4 fixtures, 4 dry-runs, 4 doc-first graders → 3 real fixes (Phase 1 had no bare-key/no-body stop instruction; `business_trace` had no scale-down carve-out unlike sibling `transition_requirements`; Output Format's closing paragraph didn't say which fields tolerate entries on a clean `ready` verdict). 3 fresh-context whole-file re-reviews (not diff) caught 4 more issues the fix rounds introduced, converging by round 3 — a 4th confirming instance of this repo's "fix ships a new contradiction" pattern (task-prep v0.68.31, decide v0.68.32, address-review v0.68.35). Mid-loop, `advisor()` caught a miscalibration in Round 2's own fix: grouping `business_trace`/`transition_requirements` with `non_functional_requirements`/`dependencies_and_risks` as "informational, doesn't block ready" was backwards, since the first two are already gated to skip flagging on trivial changes — a fired flag on either is by construction a judged-real gap, corrected before Round 3 shipped. Optimize: 2 staff-eng reviewers converged that only 1 of 9 eval items actually discriminates on the fetch-boundary hypothesis (the rest are description-invariant baseline/competitor checks); live-ran the reused `detect_trigger.py`, found and fixed a real bug (it only recognized `"Task"` as the dispatch-tool name, missing this environment's actual `"Agent"` name — silently burning 180s timeouts on 3 of 9 items). The corrected run **disproved the fetch-boundary hypothesis**: the model fetches via `jira-acli:acli` before dispatching, never on a bare key — a separate, already-existing mechanism (CLAUDE.md's Jira routing rule + the live `kbg:jira-route-nudge` hook) closes the gap upstream of the agent's own description text. Shipped no description change — Rule 2, no demonstrated problem.
- **v0.68.36** — `skill-creator:skill-creator` improve+optimize loop run against `agents/silent-failure-hunter.md` — first time against an **Agent** (Task-tool dispatch) rather than a Skill/command, per user request to delegate the eval-set-review step to staff-eng agents. Improve: 4 fixtures targeting distinct Evidence Gate claims, 8 dry-runs, 4 reviewers → 6 real doctrine gaps fixed (documented-suppression carve-out satisfied by an unverified call-site claim instead of a suppression-site reason; Gate #4 read as sufficiency not an effort floor; no severity tie-break; no cross-finding fix-coherence check — caught live, one finding's fix snippet reintroduced a bug its sibling finding had already flagged; Hunt Target 5 had no error-vs-postcondition scope boundary; runtime-flag check named no lookup locations or fallback). 2 fresh-context whole-file re-reviews (not diff) caught 5 more issues the fixes themselves introduced, converged by round 3. Optimize: `advisor()` flagged `run_loop.py` only detects the Skill tool, not Task/Agent dispatch — its proposed workaround was superseded when an eval-set reviewer found this repo already solved it for a sibling agent (`requirement-analyst-workspace/optimize/detect_trigger.py`). Added the fleet's only missing "Use when" routing clause; first draft scoped it to "changed" (diff-only) but both reviewers converged on it wrongly excluding 4 of 8 positives that were existing-code audits — this session's own fixtures were whole-file audits — fixed by dropping the scoping, not relabeling the eval set. Reconciled both reviews into a 21-query set on the `requirement-analyst` schema; skipped live execution (would require ship-then-measure) with the reason logged, matching v0.68.29/.32 precedent.
- **v0.68.35** — `skill-creator:skill-creator` improve+optimize loop run against `commands/address-review.md`, per user request to delegate the eval-set-review step to staff-eng agents. Improve half: 5 fixtures/dry-runs/graders → 12 findings, 10 fixed (1 dropped as a fixture artifact 3 graders had each independently mis-triggered on, not a real cross-confirmation; 1 deferred — different command's doc); 2 more fresh-context re-review rounds (whole file, not diff) caught 10 more issues the fix rounds themselves introduced, converging by round 3. Optimize half (the part named explicitly): 2 independent staff-eng agents blind-labeled an 18-query eval set against `flow-nudge.sh`'s regex; both converged on a real carve-out gap (a bounded "implement the reviewer's requested changes" wrongly fell through to plan-first) — fixed with a narrow exemption; live-testing surfaced 2 more confirmed recall gaps from a too-narrow distance window (15→18 chars, empirically chosen — window=20 already false-positived on "design review meeting"). 2 gaps deliberately deferred with reasons stated inline (reversed-order matching, broader Thai triggering — both riskier than the gap is worth). `test-flow-nudge.sh` 77→88, gauntlet green.
- **v0.68.34** — `skill-creator:skill-creator` improve+optimize loop run against `commands/ship-merge.md` (`disable-model-invocation: true`, so both halves were reframed: fixture-based dry runs instead of live invocation, staff-eng review of an advisory-text trigger set instead of `run_loop.py`). 5 scenarios, each independently graded by a fresh-context agent that derived the correct verdict from doc text before reading the with-skill output. 2 real fixes in the scored gate: the verified-N/A renormalization formula carried a stray `× 100` contradicting its own worked examples (100x overcompute if applied literally); the fatal-weakness floor's default behavior (any criterion below 40 fails regardless of weighted sum) was never stated as a general rule, only one named exemption existed — a dry-run agent reached the correct STOP on a stale-review scenario by elimination, not by citing stated text, exactly the "right verdict via an unstated rule" pattern the grading rubric was built to catch. 3/5 scenarios graded clean. A fresh-context re-review (per this repo's confirmed-2x lesson that a fix can ship a new contradiction) caught the floor-rule fix itself contradicting the very next paragraph — fixed with one clarifying clause. Two independent eval-set reviewers audited an 18-query recommendation-trigger set — converged on some fixes, disagreed on one (resolved via the better-argued case), and one reviewer caught a headline gap the other missed entirely (zero coverage for the automation-bias guard, arguably the command's most distinctive judgment call) — left that query's expected label as an explicit open question rather than forced to a single answer. Deliberately skipped running the reconciled set live: it would measure generic assistant phrasing, not anything the command doc controls.
- **v0.68.33** — Follow-up on v0.68.32, same session: `probe` was the one `decide` mode the main eval batch skipped. User asked for a targeted follow-up, scoped light — one dry-run pair + one reviewer, not a full batch, matching effort to a mode with ~0 real-world invocations. Prompt: a contested-diagnosis scenario (3 teams, 3 competing theories for a conversion drop). With-skill correctly picked `probe` and outperformed baseline on systems-level reasoning, but baseline independently caught a 4th candidate cause (the payment step) the with-skill run missed. Traced to a real gap: Step 1 (map the system) named the payment processor as an actor, but Step 3 (stress-test) only checked the *stated* theories, with no instruction tying it back to Step 1's full actor list — the skill's own artifact held the answer and the next step never asked for it. Fixed: Step 3 now cross-checks every mapped actor, not just the named theories. (Baseline's other lead, a canary/holdback-test suggestion, was correctly out of scope — probe's Output step hands off a framing memo, not a fix.)
- **v0.68.32** — `skill-creator:skill-creator`'s full improve+optimize loop run against `decide/SKILL.md`, same agents-only-review substitution as v0.68.28–31. 4 mode-targeted evals (decide, critique, clarify, strategize), dry-run pairs, 4 independent reviewers. The strategize eval traced a real bug (bundled "auth+billing" diagnosis where the baseline split it correctly) to a genuine precedence gap between clarify's "resolve scope first" and the triad's "one-way door → strategize" — fixed with an explicit first-match-wins mode-selection table. 9 more real fixes: strategize expanded 3→6 steps to match what `strategic-judgment.md` already documented (drift, not style); critique's output now names what would change on a non-clean verdict; completion criterion split per mode; a rung-depth contradiction resolved; `domain-modeling`/`research` citations namespaced across 2 reference docs (closing a gap v0.68.27 deferred); plus smaller precedence/citation fixes. A first fresh-context review caught 5 more issues self-review missed (including the mode table's stop-rows sitting after the working modes, silently defeating the new precedence rule). `advisor()` then caught that the fix for one of those 5 had shipped a *new* contradiction — the table routed same-session output to an external skeptic while critique's own bias-guard text claimed to handle it directly — fixed, and, per advisor's explicit instruction not to commit-then-follow-up again, a second fresh-context review ran against the file as it then stood (not the diff), catching 3 more real gaps and correctly rejecting a 4th candidate as not a real issue. Skipped description-optimization deliberately: `decide` is rare-trigger by design, ~0 real-world invocations across multiple prior audits.
- **v0.68.31** — Follow-up on v0.68.30, same session: `advisor()` flagged the 4 shipped fixes only got my own re-read, no fresh-context agent ever saw the edited `SKILL.md`. Dispatched one cold — found 2 of 4 spots clean, 2 real issues. Fix 1: the new overflow-priority paragraph and the pre-existing "fold Step 3.5's `open_questions` in" paragraph both said the same thing 4 lines apart, with the first presupposing a fact the second hadn't stated yet — merged into one paragraph, fact then rule. Fix 2: the new topical-sanity-check's mismatch note said to "fold into Step 6 as an open question" but explicitly skips dispatch, so there's no `open_questions` field to fold from — made explicit that the note rides the same batch mechanism despite no dispatch. Also dropped an inaccurate claim that Step 12's pre-send checklist picks up overflow leftovers (it doesn't check for them).
Older releases (including v0.68.27's 7-skill optimize pass, v0.68.28's eval-driven `orchestrate` loop, v0.68.29's tech-humanize loop, and v0.68.30's task-prep improve+optimize loop): see `CHANGELOG.md` for the full record.
