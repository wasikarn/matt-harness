# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Validation (run before committing)

```bash
claude plugin validate . --strict
```

Plugin manifest is the primary validation gate. `scripts/run-gauntlet.sh` runs plugin-validate + full shell-lint + JSON lint + harness-audit + the hook behavioral suite (`hooks/tests/test-gates.sh` + `test-flow-nudge.sh` + `test-session-stop.sh` — deny-gate + advisory-sensor + session/stop-hook unit tests) in parallel. The broader fleet critical-hooks suite and the eval dataset gate are pending rebuild.

## Adding or removing a surface

Auto-discovered directories: `agents/`, `skills/`, `commands/`, `hooks/`, `output-styles/`, `themes/`. See `skills/add-surface/SKILL.md` for the step-by-step (manifest bump, validation, BOUNDARY.md regen).

## Git hooks

Hooks live in `git-hooks/` (not `.git/hooks/`). Wire once per clone:

```bash
git config core.hooksPath git-hooks
```

pre-commit: fast gate — syntax/lint (`bash -n` + shellcheck), JSON validation, CRITICAL harness-audit (graceful-skip if absent).
pre-push: full gauntlet (all validation layers in parallel).

## Composer-not-creator doctrine

Before writing a new skill, command, or agent from scratch, check sources in this order: **(1)**
`mattpocock/skills` first — `claude plugin list` / the Skill tool's own available-skills list for
what's already installed under the `mattpocock-skills` plugin, plus the local clone at
`/Users/kobig/Codes/Personals/mattpocock-skills` for what's upstream-available but not yet
installed. This is a **Matt-Pocock-first harness** (his skills installed as the
`mattpocock-skills@mattpocock` plugin, namespaced `mattpocock-skills:<name>` — see README.md Quick
Start; migrated off the earlier unnamespaced `gh skill` installs 2026-07-17); checking
ECC/superpowers before matt's own repo gets the priority backwards. **(2)** the upstream ECC repo
at `/Users/kobig/Codes/Personals/ECC` and
the vendored superpowers checkout at `/Users/kobig/Codes/Personals/superpowers`. **(3)** sibling
harnesses under `/Users/kobig/Codes/Personals/` worth a structural-pattern check even when they're
not kbg's primary composer sources (e.g. `oh-my-claudecode` — cherry-picked before, ask if
unsure which repos currently qualify). Cherry-pick and adapt from whichever source fits; create
kbg-native surfaces only when none do. Confirmed gap (2026-07-17): `code-implementer`/`/implement`
were built checking only (2), skipping (1) — collided with matt's own `engineering/implement`
skill, caught by the user, not by this checklist. (A hand-pinned HEAD hash is structurally doomed
to re-stale; the path is the stable anchor — run `git rev-parse HEAD` there when you need the
current commit.)

## Architecture

The plugin ships as `kbg@kobig` from the `wasikarn/kbg-harness` GitHub repo. Claude Code loads all surfaces from `~/.claude/plugins/cache/kobig/kbg/<version>/` at startup. Nothing is symlinked.

**Doctrine injection:** `hooks/session/doctrine-bootstrap.sh` fires on SessionStart and injects `docs/METHODOLOGY.md` (decision-sizing triad + reasoning scaffold) into session context via `$CLAUDE_PLUGIN_ROOT` (the plugin install dir; the older `$CLAUDE_PLUGIN_DIR` name is not a real CC variable and expands empty).

**Operating model:** deny the irrecoverable set computationally (gates in `hooks/gates/`), advise on the rest (sensors in `hooks/advisory/`). Advisory sensors never emit `permissionDecision`. The L2–L5 autonomy ladder is retired.

**Why — the unifying crux:** the gate is a *verifier* (deterministic shell returning a branchable **score**), the model is the *maker*, and the maker can never grade its own work — an LLM judging its own output is circular ("two optimists agreeing"). So advisory sensors journal but never gate, and the autonomy ladder had to retire: a model-as-gate is the maker appointing its own verifier. **Score, not feel** — every loop's stop condition must be a number a deterministic gate can branch on, never a vibe the model rationalizes. (This is the agent-loop verifier-separation principle; see `docs/research/` + the retired L2–L5 build for the proven failure it prevents.)

When hooks are wired: gates/ (deny), advisory/ (journal), session/ (inject), stop/ (cost tracking).

## Skill authoring doctrine (matt-pocock)

When creating or editing a skill under `skills/`, follow matt-pocock's `writing-great-skills` doctrine (leading word, ≤25-word description, completion criterion, no-op test, two-cuts, failure-mode guard) — canonical: the `mattpocock-skills:writing-great-skills` skill. See `docs/skill-authoring-conventions.md` for the kbg-specific additions on top of that (harness-audit check 36's proxy coverage, Named Model footers, Suggested next step footers, AskUserQuestion-escalation criteria) — only load it when actually authoring or editing a skill/command/agent's content.

## Branching model

Single branch: `develop` only. No feature branches. Commit and push direct.

**Computationally enforced** by `gate:worktree:develop-only` (`WorktreeCreate` event) and the `git worktree add -b` block in `gate:bash:irrecoverable` (`PreToolUse:Bash`). Both gates are opt-in per repo via the `/.kbg-no-worktree` sentinel — present in the kbg-harness repo, absent from tathep/ECC/scratch repos (which keep their existing `gate:write:worktree-guard` redirect). Detached `review-pr-<N>` worktrees in `$TMPDIR` are explicitly allowlisted so the Phase 2 PR-by-number review path keeps working.

## Non-obvious gotchas

Grouped by behavior area (not a flat bucket — find the group first, then the line).

### Repo & commit hygiene

- **Hardcoded home paths blocked:** `.sh`/`.py` files must use `$HOME` or `~`, never `/Users/<name>`. The pre-commit gate will reject the commit.
- **Never `rm -rf`:** use `trash` for deletions.
- **Never `--no-verify`** on commits or pushes.
- **Stage by name:** never `git add -A` or `git add .`.

### Plugin lifecycle & install

- **`defaultEnabled: false`:** plugin ships disabled. After install, add `"kbg@kobig": true` to Claude Code `settings.json`, then restart.
- **Cache-invalidation:** same-version edits are no-ops. Always bump both manifests before `claude plugin update`. CLAUDE.md-only edits skip the bump — it's dev-facing repo guidance, not cached plugin content (only `agents/`/`skills/`/`commands/`/`hooks/`/`output-styles/`/`themes/` ship per-version).
- **Re-verifying a same-session edit to any surface:** don't hand a fixture-loop re-verification agent a name-based reference to test an edit that hasn't been bumped/reinstalled yet — not `Skill(<name>)`, not `subagent_type: <name>` for a plugin-scoped Agent, and not the slash-command itself for a Command. `agents/`, `commands/`, and `skills/` all ship inside the same single versioned bundle (`~/.claude/plugins/cache/kobig/kbg/<version>/` — confirmed identical layout across all three, 2026-07-27), so any of those resolutions silently tests stale cached content with no error, regardless of which surface type was edited. Confirmed 2026-07-27 (tech-humanize v0.68.59): one re-verification agent got a false "fix confirmed" this way via `Skill(kbg:tech-humanize)`; instruct the agent to `Read` the repo path directly instead.
- **`BOUNDARY.md` regen:** the script writes to STDOUT, not the file. The `> BOUNDARY.md` redirect is required every time.
- **Output style:** `output-styles/staff-eng.md` is the sole live-response register — self-calibrates terse vs full decision-framing by stakes, not by switching files (the old `senior-eng.md`/`staff-eng.md` two-file split was collapsed 2026-07-02; the internal "Calibrate to stakes" rule replaces the escalation/fallback dance). `force-for-plugin: true` auto-activates it whenever `kbg@kobig` is enabled, overriding the user's own `outputStyle` setting — no `/output-style` selection needed, but it also means you can't run a different style while this plugin is on without disabling it first.

### Session environment quirks

- **Working frames:** `contexts/` holds `dev.md`, `review.md`, `research.md` — loaded by `/frame` to set session posture.
- **`grep` is aliased** to `rtk grep` in this environment. Use `/usr/bin/grep` or `awk` for count/stat operations.

### Skill/agent/command mechanics & routing

- **Skill descriptions load on every Task spawn** (~words×1.3 tokens). Keep descriptions ≤25 words.
- **Thinking models:** default is the triad + `advisor()` inline (METHODOLOGY Rule 1) — `kbg:decide` is on-demand only, for genuinely hard/contested-diagnosis choices (de-scoped 2026-07-02, v0.21.4: 0 real-world invocations vs 55 `advisor()` calls across 182 sessions). The 39 on-demand mental-model files live in `docs/reference/thinking-skills/skills/` (never move to `skills/` — would break fleet count).
- **`disable-model-invocation: true`:** carried by 2 skills currently — `recursive-improve` and `score-decision` (re-check via `grep -rl "disable-model-invocation: true" skills/*/SKILL.md`; the count drifted hard after v0.46.0 moved 17 flag-carrying matt-origin skills out of this repo, and nobody updated this line). Both are now **CRIT**-guarded against the flag being silently dropped: `recursive-improve/SKILL.md` by check 39, `score-decision/SKILL.md` by check 49 (added 2026-07-23 after a `kbg:plan-reviewer` pass on a skill-improvement batch plan flagged that skill-creator's own description-optimizer rewrites SKILL.md frontmatter, and only recursive-improve had a real gate). Check 30 still only WARNs that a `-reason` field exists on whichever skills currently carry the flag — it's the presence-of-reason check, not the flag-survives-a-rewrite check; the two CRIT checks above are what actually close that gap now.
- **`review_mode` in `ship-merge`:** `review-pr` tags its state write `pr-by-number` (isolated worktree) or `own-branch` (self-review). `ship-merge` caps the Critical-findings score at the fatal-weakness floor on sensitive-path diffs reviewed `own-branch` — an automation-bias guard against trusting a same-session self-review's severity tiering. "Sensitive-path" covers both auth/secret/credential/payment/billing/token AND the harness's own verifier/gate code (`hooks/gates/**`, `hooks/hooks.json`, `skills/harness-audit/scripts/{audit.sh,checks/**}` — the same list `hooks/gates/verifier-protect.sh` protects).
- **`orchestrate` vs `kbg:decide`:** orchestrate decides whether/how to spend effort on an ask (inline/parallel/sequential/drop, which surface receives it) *before* it's understood as a bounded decision; `decide` reasons through a bounded question once you're already committed to answering it. A pile of competing asks routes through `orchestrate` first; a single reversible-choice question goes straight to `decide`.
- **`SKILL.md` frontmatter ≠ `agents/*.md` frontmatter:** the real skill-file field for tool control is `allowed-tools` (pre-approves without asking; there's no hard-restriction field for skills), not `tools:` — that's the `agents/*.md` subagent field (hard-restricts to a fixed set). Confirmed against the official 16-field `skills.md` reference and the shipped CLI binary's own compiled schema key list. `metadata` / `metadata.origin` / `disable-model-invocation-reason` are non-standard-but-harmless kbg conventions — Claude Code tolerates unrecognized frontmatter keys (confirmed via changelog + `plugin.json`'s own documented "unrecognized fields → warning, not error" policy), they just carry zero behavioral effect.
- **`/goal` vs `goal-craft`:** `/goal` is Claude Code's own native completion-condition loop (v2.1.139+, session-scoped, judged each turn by a separate small model that reads only the transcript). kbg never wraps or auto-invokes `/goal` itself — the user always types `/goal` themselves, no exceptions. `skills/goal-craft/SKILL.md` only composes a paste-ready condition string (mandatory one-way-door screen + turn bound) and, as of 2026-07-08, is model-invocable (its `disable-model-invocation` flag was removed on user request) — the model may draft a condition unprompted, but the string is inert until the user pastes it after `/goal`. Auto-dispatch of `/goal` itself (`claude -p "/goal ..."`) is still a deliberate non-goal — it forks a separate headless session and reopens the retired L4/L5 "no model self-start" invariant.

## Recent versions

Quick orientation for the last few releases. For full notes see `CHANGELOG.md`.

Cap this list at 10 bullets. Keep the last 3-5 releases as individual entries; fold older ones into version-range bullets (`vX.Y.a–vX.Y.b` or `vX.Y.x`) by theme. Adding an 11th bullet means dropping the oldest — `CHANGELOG.md` is the full record, this section is orientation only.

- **v0.68.82** — Closed 2 deferred gaps in `ship-merge-workspace`: no fixture had an intermediate CI score value or a wholly missing review-state file. Built `scenario-6-ci-partial-pending` and `scenario-7-missing-review-state` (split-format fixtures + a pre-registered expected analysis written before dispatch), then ran 2 independent reviewers against both (doc + fixture data fully inlined, zero access to the answer key). Both matched the pre-registered arithmetic exactly and both reached STOP — but both also independently surfaced 2 real doc gaps (CI status's Measures wording has no explicit binary/no-partial-credit clause like Review coverage does; step 3's "must pass" reads like an independent Phase-1 stop when it actually just feeds step 6's scored table) and caught a blind spot in my own pre-registered ground truth: Review freshness also fails when no review-state file exists, not just Critical-findings and Review-coverage. Verified all 3 via `grep` before crediting anything to the doc, then fixed all 3 in `commands/ship-merge.md` — the first pass on this workspace since v0.68.34/.76/.77 to land more than one fix, since passes 1-3 had already converged the 5 original scenarios to a stable, correct state.
- **v0.68.81** — Fixed the real limitation v0.68.80 had documented but not built: `cost-tracker.sh` recorded one whole-session cumulative counter per stop, repriced at whichever model was currently active, so "By model" always attributed 100% of a mixed-model session's cost to whichever model was active at its last stop. The hook already re-parses the full transcript statelessly on every stop; the fix groups assistant turns by `.message.model` before summing instead of tagging one whole-session sum with `last.m` — writes one row per model actually used, each carrying that model's own true cumulative tokens/cost, marked `model_scoped: true`. Verified the grouping wasn't a no-op first: a real 3-model transcript confirmed `.message.model` genuinely varies per turn (227/111/30 messages across opus/sonnet/glm), ran the new jq unredirected (no silent-swallow risk) and hand-verified the per-model costs, then ran the actual hook end-to-end against a scratch metrics dir and confirmed the appended rows matched exactly. Updated `cost-report.md`'s aggregation: sessions with any `model_scoped` row dedup by (session,model) and sum (safe now — a `model_scoped` row recomputes from the whole transcript, so it supersedes anything older); sessions with none still fall back to the old dedup-by-session-only path, regression-gated byte-identical against real production data before and after. Caught a real test-coverage gap: the existing hook test suite only used single-model transcripts, so the grouping change was a no-op for every existing assertion — added a genuine multi-model adversarial case (two rows, two models, tokens never summed together).
- **v0.68.80** — Chased down v0.68.79's `advisor()`-flagged question about multi-model cost attribution — and initially got it wrong. Row patterns (cost dropping on model switch, jumping back on resume) looked exactly like independent per-(session,model) counters, so `cost-report.md`'s dedup was rekeyed to (session,model) and summed, "fixing" the total from $34,698 to a fabricated $53,883 (committed, unpushed). `advisor()` on the next pass didn't re-analyze the same row patterns — it asked to read the actual writer, `hooks/stop/cost-tracker.sh`. Its `jq` sums every assistant message's tokens unconditionally, no model filter, tagging each row with only the most-recent message's model: one whole-session cumulative counter, repriced at whichever model is currently active — not independent per-model counters. Confirmed empirically: `input_tokens` is monotonically non-decreasing across all 32 multi-model sessions in production data, zero exceptions. The decisive check: a delta-based per-session recomputation (cost[i]−cost[i−1], summed) landed on exactly $34,698.3252 — a telescoping-sum identity proving the original total was correct all along, and the "fix" was double/triple-counting the same tokens on every model switch. Reverted the dedup key to `session_id` alone. The real, narrower bug survives: the "By model" breakdown mis-attributes a mixed-model session's entire cost to whichever model was active at its last stop (~9% of sessions, 32/362) — tried a delta-per-row attribution with negative deltas clamped to 0, but that overstated the total by 31% (the negative deltas are real repricing signal, not noise) and was discarded; documented as a known limitation instead, since fixing it for real needs `cost-tracker.sh` to accumulate cost per model going forward, not a report-side aggregation trick. Rewrote the CHANGELOG's own v0.68.79 entry and the backing memory file, both of which had recorded the wrong claim as fact.
- **v0.68.79** — Picked `commands/cost-report.md` as the next `/review-fixtures` target (an Explore agent found 14 never-touched fleet surfaces; picked this one because, like `review-pr`, it has real local production data to mine directly: `~/.local/share/kbg/metrics/costs.jsonl`, 5985 rows). Found and fixed a real UTC-vs-local timezone bug in the "today"/"yesterday" buckets (reproduced live at 01:51 local in UTC+7, still 2026-07-27 UTC — unfixed report showed "today: $972.84" when the real local-today total was $376), fixed by switching to `Date`'s local getters. `advisor()`, consulted before push, flagged the fix as itself TZ-environment-dependent (confirmed: `TZ=UTC` reverts the exact bug — correct for the realistic interactive case) and asked whether multi-model sessions were attributed correctly in the "By model" breakdown — chased down, and initially answered wrong, in v0.68.80 above.
- **v0.68.78** — Fleet-count drift fix found while researching the next `/review-fixtures` target: `plugin.json`/`marketplace.json`/`README.md` all claimed "18 commands" but `ls commands/*.md` shows 16 — several commands were deleted across prior releases without the hardcoded count string being updated each time (same class of drift v0.68.72 fixed for the agent count). No source-of-truth script generates these counts; fixed all 3 files to "16 commands." No new `harness-audit` check added per Rule 2 — one drift instance isn't yet a proven-recurring pattern.
- **v0.68.77** — Picked `review-pr` as the next `/review-fixtures` target; mined its 105 real production `review-pr-*.json` state files directly instead of dispatching fixtures first. Found the `rehunt` field — one of the 7 keys `/ship-merge`'s gate contract depends on, documented to hold exactly 4 canonical values — actually holds 15+ distinct shapes in real files (free-text narrative, `not_triggered`, `complete`), with 20/105 (≈19%) missing it entirely. `advisor()` flagged that the literal string `"incomplete"` never appearing in the corpus was ambiguous between "never happened" and "happened, recorded differently" — resolved by checking every file's `dispatch_failures` field: always `0`/`[]`, confirming zero real dispatch failures ever occurred, so this is confirmed schema drift with no evidence the incomplete-review guard was ever actually bypassed. Root cause: `REHUNT_STATUS`/`DISPATCH_FAILURES` are freeform bash variables constrained only by a prose comment, zero code enforcement. First fix — a `case` canonicalization in `skills/review-pr/SKILL.md`'s Phase 7 write block — only covers sessions that run the printf script at all; `advisor()` caught that ~19% of real files (missing `rehunt` entirely) can't have come from that script, since it hardcodes the key. Added the fix that actually covers both populations: `commands/ship-merge.md`'s Incomplete-review guard now treats a missing-or-non-canonical `rehunt` the same as an explicit `"incomplete"` — STOP regardless of which session produced the file.
- **v0.68.76** — Second /review-fixtures pass on `ship-merge`: found the workspace already had `findings.md` (v0.68.34, 3 fixes applied and confirmed live), so scoped this round to a genuinely bare-prompt test instead of re-deriving those. First attempt (2 reviewers reasoning against the 5 combined fixture files with an "ignore the Task section" instruction) failed — both independently caught that reading the file loads the scaffolding before "ignore" can take effect, a 3rd confirmed instance of the fixture-contamination pattern (memory updated). Fixed by splitting each fixture into a data-only file + a scaffolded-task file + one shared scenario-agnostic bare task, and re-dispatched with everything inlined (data + doc text) so no filesystem access was needed for Pass 1 at all — closing even the filename-leak vector (`scenario-5-freshness-mismatch.md` names the mechanism before it's opened). Result: 5/5 verdict matches across both reviewers, zero divergence, confirming the prior fixes hold under real bare-prompt conditions. One new, arithmetic-verified finding (single-sourced by one reviewer, independently reproduced by me before acting): the automation-bias guard's own worked example already showed both numbers (50 for no-CI, ≈64.7 for real-CI) side by side but never named the reason — excluding a passing criterion from the denominator concentrates the zeroed criterion's proportional weight, so a no-CI repo is a *stricter* case for a self-tiered sensitive review, not a more lenient one. Added one clarifying sentence at that exact spot, same pattern as the freshness case's existing callout.
- **v0.68.75** — Ran a second `/review-fixtures plan-reviewer` reconciliation pass on the existing `plan-reviewer-workspace/iteration-1` outputs, unaware a first pass had already happened: `git diff HEAD -- agents/plan-reviewer.md` came back empty, meaning the template-partial-fill-on-hard-stop + lens-mistagging fixes already existed at HEAD (275df7c, v0.68.46, an undocumented-as-fixture-loop prior session). Both reviewers independently re-derived the same "keep the Circularity guard strict" verdict the prior pass already recorded — corroboration, not a new finding — so `agents/plan-reviewer.md` itself got zero further edits. Two genuinely new items: fixed `harness-audit/SKILL.md`'s "Extending checks" section, which described the pre-per-file-check-script mechanism (a single `audit.sh` appending `[CRIT] <id>: <message>` lines) instead of the real one (`scripts/checks/NN-slug.sh` + shared `crit`/`warn`/`info` functions); and actually applied the fixture-c ground-truth correction the prior pass had only flagged as needing a fix ("the answer key needs correcting") but never did. Surfaced a real gap in `/review-fixtures` itself: the command never reads a workspace's existing `feedback.json` before dispatching a fresh reconciliation pass, so a second run on the same target re-derives already-recorded conclusions instead of scoping to what's uncovered — logged for a future fix, not built this round.
- **v0.68.74** — First-ever `/review-fixtures nextjs-reviewer` run, built from scratch (no existing workspace). Eval 0 (cheap regression check, no reconciliation): dispatched the fixed vs. pre-fix (v0.68.72 snapshot) agent against the same `proxy.ts` fixture — confirmed empirically that the pre-fix agent's own scope filter excludes `proxy.ts` and approves with zero findings despite 2 planted bugs, while the fixed agent catches both. Eval 1 (full 2-reviewer loop): baseline = a generic reviewer with zero Next.js-specific instructions, to measure real incremental value — result was nuanced, baseline nearly matched the specialist on raw bug-catching (missed nothing decoy-wise, independently caught 2 real gaps the specialist missed), while the specialist's actual edge was doctrine-anchored severity calibration, not coverage. Both independent reconciliation reviewers, verified against live Next.js docs, converged on one real bug: the specialist's canonical IDOR finding claimed Next.js encrypts `.bind()` Server Action arguments — backwards, `.bind()` args are the documented *unencrypted* opt-out. Grepped the file first (per the reconciliation contract) and confirmed it never mentioned `.bind()` at all — a content gap, not a wrong assertion, that let the agent invent a risk-understating explanation. Fixed both that gap and a second double-confirmed one (no doctrine on unchecked `fetch()` failures/missing `error.tsx`). Declined 2 lower-confidence single-sourced items per Rule 2.
Older releases (v0.68.27–v0.68.73): a sustained `/review-fixtures` improve+optimize sweep across the fleet — build-error-resolver, refactor-cleaner, security-reviewer, backend-patterns/frontend-patterns builds and re-reviews, orchestrate, tech-humanize, task-prep, decide, ship-merge, address-review, silent-failure-hunter, requirement-analyst, typescript-reviewer, summarizer, blind-spot-hunter, code-implementer (×2), code-reviewer, typescript-patterns, python-reviewer, performance-optimizer, and nextjs-reviewer (a factual/scoping-bug pass — 4 fixes, including a `proxy.ts` scoping gap that would've silently skipped the file its own Middleware guidance covers), plus the `/review-fixtures` command's own workspace-schema generalization (v0.68.62/.63) and self-check (v0.68.61). Each loop found and fixed 1-5 real bugs, verified against fixtures or live tools (Postgres, Node, `tsc --strict`). Also: `flutter-reviewer` removal (v0.68.72) and two Tathep framework-pattern removal batches (`adonisjs-patterns`/`dart-flutter-patterns`/`effect-ts-patterns`/`fastapi-patterns`/`hono-patterns`/`langchain-langgraph-patterns`/`tauri-v2-patterns` in v0.68.51, then `codebase-onboarding` in v0.68.52). See `CHANGELOG.md` for the full per-release record.
