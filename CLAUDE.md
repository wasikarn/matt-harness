# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Validation (run before committing)

```bash
claude plugin validate . --strict
```

Plugin manifest is the primary validation gate. `scripts/run-gauntlet.sh` runs plugin-validate + full shell-lint + JSON lint + harness-audit + the hook behavioral suite (`hooks/tests/test-gates.sh` + `test-flow-nudge.sh` + `test-session-stop.sh` — deny-gate + advisory-sensor + session/stop-hook unit tests) in parallel. The broader fleet critical-hooks suite and the eval dataset gate are pending rebuild.

## Adding or removing a surface

Auto-discovered directories: `agents/`, `skills/`, `commands/`, `hooks/`, `output-styles/`, `themes/`. See `skills/add-surface/SKILL.md` for the step-by-step (manifest bump, validation, BOUNDARY.md regen).

## Git hooks

Hooks live in `git-hooks/` (not `.git/hooks/`). Wire once per clone:

```bash
git config core.hooksPath git-hooks
```

pre-commit: fast gate — syntax/lint (`bash -n` + shellcheck), JSON validation, CRITICAL harness-audit (graceful-skip if absent).
pre-push: full gauntlet (all validation layers in parallel).

## Composer-not-creator doctrine

Before writing a new skill, command, or agent from scratch, check sources in this order: **(1)**
`mattpocock/skills` first — `claude plugin list` / the Skill tool's own available-skills list for
what's already installed under the `mattpocock-skills` plugin, plus the local clone at
`/Users/kobig/Codes/Personals/mattpocock-skills` for what's upstream-available but not yet
installed. This is a **Matt-Pocock-first harness** (his skills installed as the
`mattpocock-skills@mattpocock` plugin, namespaced `mattpocock-skills:<name>` — see README.md Quick
Start; migrated off the earlier unnamespaced `gh skill` installs 2026-07-17); checking
ECC/superpowers before matt's own repo gets the priority backwards. **(2)** the upstream ECC repo
at `/Users/kobig/Codes/Personals/ECC` and
the vendored superpowers checkout at `/Users/kobig/Codes/Personals/superpowers`. **(3)** sibling
harnesses under `/Users/kobig/Codes/Personals/` worth a structural-pattern check even when they're
not kbg's primary composer sources (e.g. `oh-my-claudecode` — cherry-picked before, ask if
unsure which repos currently qualify). Cherry-pick and adapt from whichever source fits; create
kbg-native surfaces only when none do. Confirmed gap (2026-07-17): `code-implementer`/`/implement`
were built checking only (2), skipping (1) — collided with matt's own `engineering/implement`
skill, caught by the user, not by this checklist. (A hand-pinned HEAD hash is structurally doomed
to re-stale; the path is the stable anchor — run `git rev-parse HEAD` there when you need the
current commit.)

## Research: check qmd before web search

Before starting primary-source research — fact-checking a claim, investigating a library/API,
verifying a citation — search the local `qmd` collections first, then query `context7` for any
library/framework doc lookup, before reaching for `WebSearch`. Relevant collections: `kbg-research`
(this repo's own `docs/research/`), `kbg-memory` (this repo's own memory store), `llm-wiki` (the
operator's personal knowledge vault, 1,600+ docs spanning every project), plus the tathep/dotfiles
collections. Run `qmd status` (or the `status` MCP tool) for the full current list; scope a query
to the relevant collections rather than searching all of them blind.

**Why this line lives here, not in a skill:** kbg built exactly this qmd-first behavior once
already, wired into a vendored `research` skill (v0.44.0, explicitly kept vendored so "a bare
install would lose" the qmd/context7 fold-in). It was deleted anyway — commit `d99ccf3` swapped it
for a bare `gh skill` copy, then v0.58.11 swapped that for the upstream `mattpocock-skills:
engineering/research` plugin skill, which carries no qmd or context7 awareness (confirmed by grep,
2026-07-30). Two unrelated migrations, both aimed at fixing namespace collisions, silently dropped
this value with nobody catching it. A skill file is exactly the kind of surface an upstream resync
can overwrite out from under you; `CLAUDE.md` isn't. The rule lives here so it survives the next
resync and applies to every research-shaped task, not just the ones that happen to route through
one particular skill.

**Cross-project reach (v0.68.106):** this file only loads when cwd is `kbg-harness` — a
foreign-project session never sees this rule. The operator's own `~/.claude/CLAUDE.md`
(dotfiles-owned, not shipped with this plugin) now carries a short mirror of the `llm-wiki`
half of this rule, so the vault stays reachable from any project. That mirror doesn't replace
this section — it's a thinner pointer for a context this file can't reach. `/kbg:wiki-ingest`
and `kbg:wiki-scan` (this repo) give the vault a user-invoked write path and a read-only health
check, respectively; see their own files for the doctrine reasoning (public-plugin-touching-
private-vault, graceful-skip when `~/llm-wiki` is absent, `disable-model-invocation` on the
mutating command).

## Architecture

The plugin ships as `kbg@kobig` from the `wasikarn/kbg-harness` GitHub repo. Claude Code loads all surfaces from `~/.claude/plugins/cache/kobig/kbg/<version>/` at startup. Nothing is symlinked.

**Doctrine injection:** `hooks/session/doctrine-bootstrap.sh` fires on SessionStart and injects `docs/METHODOLOGY.md` (decision-sizing triad + reasoning scaffold) into session context via `$CLAUDE_PLUGIN_ROOT` (the plugin install dir; the older `$CLAUDE_PLUGIN_DIR` name is not a real CC variable and expands empty).

**Operating model:** deny the irrecoverable set computationally (gates in `hooks/gates/`), advise on the rest (sensors in `hooks/advisory/`). Advisory sensors never emit `permissionDecision`. The L2–L5 autonomy ladder is retired.

**Why — the unifying crux:** the gate is a *verifier* (deterministic shell returning a branchable **score**), the model is the *maker*, and the maker can never grade its own work — an LLM judging its own output is circular ("two optimists agreeing"). So advisory sensors journal but never gate, and the autonomy ladder had to retire: a model-as-gate is the maker appointing its own verifier. **Score, not feel** — every loop's stop condition must be a number a deterministic gate can branch on, never a vibe the model rationalizes. (This is the agent-loop verifier-separation principle; see `docs/research/` + the retired L2–L5 build for the proven failure it prevents.)

When hooks are wired: gates/ (deny), advisory/ (journal), session/ (inject), stop/ (cost tracking).

## Skill authoring doctrine (matt-pocock)

When creating or editing a skill under `skills/`, follow matt-pocock's `writing-great-skills` doctrine (leading word, ≤25-word description, completion criterion, no-op test, two-cuts, failure-mode guard) — canonical: the `mattpocock-skills:writing-great-skills` skill. See `docs/skill-authoring-conventions.md` for the kbg-specific additions on top of that (harness-audit check 36's proxy coverage, Named Model footers, Suggested next step footers, AskUserQuestion-escalation criteria) — only load it when actually authoring or editing a skill/command/agent's content.

## Branching model

Single branch: `develop` only. No feature branches. Commit and push direct.

**Computationally enforced** by `gate:worktree:develop-only` (`WorktreeCreate` event) and the `git worktree add -b` block in `gate:bash:irrecoverable` (`PreToolUse:Bash`). Both gates are opt-in per repo via the `/.kbg-no-worktree` sentinel — present in the kbg-harness repo, absent from tathep/ECC/scratch repos (which keep their existing `gate:write:worktree-guard` redirect). Detached `review-pr-<N>` worktrees in `$TMPDIR` are explicitly allowlisted so the Phase 2 PR-by-number review path keeps working.

## Non-obvious gotchas

Grouped by behavior area (not a flat bucket — find the group first, then the line).

### Repo & commit hygiene

- **Hardcoded home paths blocked:** `.sh`/`.py` files must use `$HOME` or `~`, never `/Users/<name>`. The pre-commit gate will reject the commit.
- **Never `rm -rf`:** use `trash` for deletions.
- **Never `--no-verify`** on commits or pushes.
- **Stage by name:** never `git add -A` or `git add .`.

### Plugin lifecycle & install

- **`defaultEnabled: false`:** plugin ships disabled. After install, add `"kbg@kobig": true` to Claude Code `settings.json`, then restart.
- **Cache-invalidation:** same-version edits are no-ops. Always bump both manifests before `claude plugin update`. CLAUDE.md-only edits skip the bump — it's dev-facing repo guidance, not cached plugin content (only `agents/`/`skills/`/`commands/`/`hooks/`/`output-styles/`/`themes/` ship per-version).
- **Re-verifying a same-session edit to any surface:** don't hand a fixture-loop re-verification agent a name-based reference to test an edit that hasn't been bumped/reinstalled yet — not `Skill(<name>)`, not `subagent_type: <name>` for a plugin-scoped Agent, and not the slash-command itself for a Command. `agents/`, `commands/`, and `skills/` all ship inside the same single versioned bundle (`~/.claude/plugins/cache/kobig/kbg/<version>/` — confirmed identical layout across all three, 2026-07-27), so any of those resolutions silently tests stale cached content with no error, regardless of which surface type was edited. Confirmed 2026-07-27 (tech-humanize v0.68.59): one re-verification agent got a false "fix confirmed" this way via `Skill(kbg:tech-humanize)`; instruct the agent to `Read` the repo path directly instead.
- **`BOUNDARY.md` regen:** the script writes to STDOUT, not the file. The `> BOUNDARY.md` redirect is required every time.
- **Output style:** `output-styles/staff-eng.md` is the sole live-response register — self-calibrates terse vs full decision-framing by stakes, not by switching files (the old `senior-eng.md`/`staff-eng.md` two-file split was collapsed 2026-07-02; the internal "Calibrate to stakes" rule replaces the escalation/fallback dance). `force-for-plugin: true` auto-activates it whenever `kbg@kobig` is enabled, overriding the user's own `outputStyle` setting — no `/output-style` selection needed, but it also means you can't run a different style while this plugin is on without disabling it first.

### Session environment quirks

- **Working frames:** `contexts/` holds `dev.md`, `review.md`, `research.md` — loaded by `/frame` to set session posture.
- **`grep` is aliased** to `rtk grep` in this environment. Use `/usr/bin/grep` or `awk` for count/stat operations.

### Skill/agent/command mechanics & routing

- **Skill descriptions load on every Task spawn** (~words×1.3 tokens). Keep descriptions ≤25 words.
- **Thinking models:** default is the triad + `advisor()` inline (METHODOLOGY Rule 1) — `kbg:decide` is on-demand only, for genuinely hard/contested-diagnosis choices (de-scoped 2026-07-02, v0.21.4: 0 real-world invocations vs 55 `advisor()` calls across 182 sessions). The 39 on-demand mental-model files live in `docs/reference/thinking-skills/skills/` (never move to `skills/` — would break fleet count).
- **`disable-model-invocation: true`:** carried by 2 skills currently — `recursive-improve` and `score-decision` (re-check via `for f in skills/*/SKILL.md; do head -20 "$f" | grep -qF 'disable-model-invocation: true' && echo "$f"; done` — frontmatter-scoped, not a bare `grep -rl`, which also matches skills that only *mention* another skill's flag in prose (e.g. `decide`/`orchestrate` cite `score-decision`/`wayfinder`'s flags without carrying one themselves) and would misreport the count; the count drifted hard after v0.46.0 moved 17 flag-carrying matt-origin skills out of this repo, and nobody updated this line). Both are now **CRIT**-guarded against the flag being silently dropped: `recursive-improve/SKILL.md` by check 39, `score-decision/SKILL.md` by check 49 (added 2026-07-23 after a `kbg:plan-reviewer` pass on a skill-improvement batch plan flagged that skill-creator's own description-optimizer rewrites SKILL.md frontmatter, and only recursive-improve had a real gate). Check 30 still only WARNs that a `-reason` field exists on whichever skills currently carry the flag — it's the presence-of-reason check, not the flag-survives-a-rewrite check; the two CRIT checks above are what actually close that gap now.
- **`review_mode` in `ship-merge`:** `review-pr` tags its state write `pr-by-number` (isolated worktree) or `own-branch` (self-review). `ship-merge` caps the Critical-findings score at the fatal-weakness floor on sensitive-path diffs reviewed `own-branch` — an automation-bias guard against trusting a same-session self-review's severity tiering. "Sensitive-path" covers both auth/secret/credential/payment/billing/token AND the harness's own verifier/gate code (`hooks/gates/**`, `hooks/hooks.json`, `skills/harness-audit/scripts/{audit.sh,checks/**}` — the same list `hooks/gates/verifier-protect.sh` protects).
- **`orchestrate` vs `kbg:decide`:** orchestrate decides whether/how to spend effort on an ask (inline/parallel/sequential/drop, which surface receives it) *before* it's understood as a bounded decision; `decide` reasons through a bounded question once you're already committed to answering it. A pile of competing asks routes through `orchestrate` first; a single reversible-choice question goes straight to `decide`.
- **`SKILL.md` frontmatter ≠ `agents/*.md` frontmatter:** the real skill-file field for tool control is `allowed-tools` (pre-approves without asking; there's no hard-restriction field for skills), not `tools:` — that's the `agents/*.md` subagent field (hard-restricts to a fixed set). Confirmed against the official 16-field `skills.md` reference and the shipped CLI binary's own compiled schema key list. `metadata` / `metadata.origin` / `disable-model-invocation-reason` are non-standard-but-harmless kbg conventions — Claude Code tolerates unrecognized frontmatter keys (confirmed via changelog + `plugin.json`'s own documented "unrecognized fields → warning, not error" policy), they just carry zero behavioral effect.
- **`/goal` vs `goal-craft`:** `/goal` is Claude Code's own native completion-condition loop (v2.1.139+, session-scoped, judged each turn by a separate small model that reads only the transcript). kbg never wraps or auto-invokes `/goal` itself — the user always types `/goal` themselves, no exceptions. `skills/goal-craft/SKILL.md` only composes a paste-ready condition string (mandatory one-way-door screen + turn bound) and, as of 2026-07-08, is model-invocable (its `disable-model-invocation` flag was removed on user request) — the model may draft a condition unprompted, but the string is inert until the user pastes it after `/goal`. Auto-dispatch of `/goal` itself (`claude -p "/goal ..."`) is still a deliberate non-goal — it forks a separate headless session and reopens the retired L4/L5 "no model self-start" invariant.

## Recent versions

Quick orientation for the last few releases. For full notes see `CHANGELOG.md`.

Cap this list at 10 individual entries plus one folded range bullet. Keep the last 10 releases as individual entries; fold older ones into version-range bullets (`vX.Y.a–vX.Y.b` or `vX.Y.x`) by theme. Growing past 10 individual entries means folding the oldest into the range bullet instead of adding an 11th — `CHANGELOG.md` is the full record, this section is orientation only.

- **v0.68.108** — Second `/kbg:review-fixtures` loop this session, for `commands/wiki-ingest.md` — the write-capable, `disable-model-invocation: true` sibling of v0.68.107's `wiki-scan` fix. `advisor()` confirmed a fixture agent running the doc's own bash steps against a disposable clone isn't what the flag blocks (that stops the model firing the real command off a chat "go," not testing the script). Built 2 evals with 4 isolated per-arm clones (closing the shared-clone contamination gap v0.68.107's own fixture note flagged) — a relative-path decoy trap that turned out not to discriminate (both arms independently avoided it by reading `ingest.sh`'s own source), and a hotcache-skip disclosure that discriminated cleanly (the baseline never mentioned `hotcache.md` at all, since `ingest.sh`'s own stdout never hints it exists). Fixed 3 real, code-verified gaps: a topic-mismatch edge case in the "What it writes" list (reproduced live before and after the fix), a missing skip-parenthetical asymmetry, and a hotcache gotcha that inferred its conclusion instead of requiring the actual `git diff --stat` output be quoted. One reviewer also caught and the orchestrator re-verified an inaccuracy in its own dispatch context — a claimed self-disclosure held for only one of two baseline arms, the other only reaching the dispatcher's return-summary, never the user-facing deliverable. Full reconciliation in `wiki-ingest-workspace/iteration-1/feedback.json`.
- **v0.68.107** — First-ever `/kbg:review-fixtures` loop for `skills/wiki-scan/SKILL.md`, a few hours after it shipped in v0.68.106. `advisor()` was consulted before building fixtures — the skill's whole job is running real scripts against the operator's real, mutable personal vault, and a no-skill baseline agent with Bash could plausibly mutate it — so both fixtures ran against a disposable `git clone` of the real vault instead, never the real one. Found a real, quadruple-confirmed bug (2 independent with-skill fixture agents + 2 independent reviewers, all reproduced live): `SKILL.md`'s preflight set `VAULT` as a plain, unexported shell variable, so the Run section's `bash "$VAULT/scripts/..."` calls spawned child processes that never saw it — both `lint-scan.sh` and `stats.sh` silently fell back to `$HOME/llm-wiki` regardless of `KBG_WIKI_VAULT`, latent since v0.68.106 shipped only because the fallback happened to equal the real vault at its default path. Fixed with `export VAULT`, plus a note that the skill's own "Verify / done when" section couldn't have caught this (a clean exit 0 happens either way). Independently verified (3 separate confirmations) that `commands/wiki-ingest.md` does **not** share this bug — its prefix-assignment invocation style exports correctly, unlike `wiki-scan`'s split-statement form. Two smaller, real, target-attributable gaps fixed alongside: an undocumented third `scripts/lint-wiki.sh` with *no* path safety at all (added to the failure-mode guard), and `lint-scan.sh` having no broken-`[[wikilink]]` check (only the excluded, mutating `lint.sh` does — a with-skill run had silently answered only 2 of a user's 3-part ask). All 3 fixes re-verified live against the scratch clone. Full reconciliation in `wiki-scan-workspace/iteration-1/feedback.json`.
- **v0.68.106** — Wired the operator's personal `llm-wiki` knowledge vault into kbg for cross-project reach, after a `kbg:orchestrate`-routed research phase (3 parallel agents: `oh-my-claudecode`'s install pattern, `graphify`'s capabilities, external 2026 second-brain architecture) found the vault already implements the Karpathy "LLM Wiki" pattern correctly and needed reach, not a redesign — kbg's own "check qmd before web search" rule only loads when cwd is `kbg-harness`, so the vault was invisible from every other project (confirmed: 0 matches for `llm-wiki`/`qmd`/`hotcache` in `~/.claude/CLAUDE.md`). Two designs were pressure-tested and rejected before this shipped: a SessionStart hook auto-injecting `hotcache.md` (killed — LLM-authored/mutable content, 13 days stale by the vault's own lint criterion, opens with a "skip further browse" directive, ~700 tok/session forever, matches the "reject apply" half of a prior ECC auto-inject study), and `graphify` replacing `qmd` (killed — no vector/semantic search at all, lexical+graph-traversal only, no auto-wired MCP for Claude Code). Shipped: a ~100-token rule added to the operator's global `~/.claude/CLAUDE.md` (dotfiles-owned, committed separately) for the always-on layer; `/kbg:wiki-ingest`, a `disable-model-invocation: true` command wrapping the vault's `ingest.sh` (doctrine-mandated per `docs/command-authoring-conventions.md`'s "hard to reverse or reaches outside the repo" criterion); `kbg:wiki-scan`, a read-only skill wrapping `lint-scan.sh`/`stats.sh`. A `kbg:plan-reviewer` pass on the approved plan (before any code was written) found and fixed 5 real issues: the graceful-skip promise had no actual preflight check in either surface's body; a same-line `VAULT="..." bash "$VAULT/..."` prefix-assignment is a live bash bug (verified by reproducing it) that silently breaks or mis-resolves; `ingest.sh`'s own hotcache-append dedup guard is already permanently tripped by existing vault content, contradicting the command's stated write-contract; the dotfiles CLAUDE.md edit needed its own commit step in a separate repo with pre-existing unrelated WIP; and a factual correction to which harness-audit check would fire on the new skill's description. All 5 closed before implementation. A fleet-count drift was also caught and fixed by `sync-fleet-counts.sh` mid-ship — the actual pre-existing baseline was 18 commands, not the 16 this repo's own docs claimed.
- **v0.68.105** — An `advisor()` pass on v0.68.104's just-shipped fix (run before declaring that loop done) caught 3 things the initial ship missed, all in `agents/task-prep-checker.md`: (1) the re-verification run for `eval-clean-ready-prompt` returned the correct `verdict: gaps` but still emitted `done_when_shape: repro` — the very shape the fix says shouldn't be credited once the premise check fails; fixed by requiring `missing` instead. (2) the re-verification dispatch prompts all carried a sentence that externally restated the Output Format fix, confounding the schema-clean result for the 2 previously-violating evals; re-ran the more severe one with that sentence removed (iteration-1c) — came back schema-clean regardless, confirming the fix holds independent of the redundant instruction. (3) one output wrote `field: <done-when>` with literal angle brackets instead of the bare field name — the same unpinned-placeholder drift closed for `claude-md-health`'s verdict column one release ago; fixed with an explicit placeholder note. All 3 are documentation-only guardrail tightenings — no change to `verdict` or `colleague_test_pass` behavior.
- **v0.68.104** — First-ever `/kbg:review-fixtures task-prep-checker` loop — no prior workspace, no fix history since creation. Ground-up build: 3 synthetic task-prep-assembled drafts targeting the agent's own documented failure modes (field/shape classification, over-reporting, the Prompt Defense Baseline). 2 reviewers converged on 2 real, double-confirmed, target-attributable gaps: Phase 4's `done_when_shape` classification credited a `repro` shape from surface features alone (a runnable command + a number) without cross-checking the check's premise against `<context>` — a CI-only-bug draft with a local-only re-run check got `verdict: ready` on a surface read; and 2 of 3 live single-shot runs violated the agent's own Output Format (prose preamble/postamble, a stray second-language summary) under complex judgment calls. Fixed both — Phase 4 now requires the premise cross-check, and Output Format/Guardrails/Anti-Patterns now explicitly forbid any text outside the 4-line schema block — re-verified live via a general-purpose agent instructed to `Read` the live-edited file directly (plugin cache not yet bumped). One reviewer's external-coaching theory on the format violation was investigated and refuted (0 tool calls on that dispatch — a single uncoached run); a second reviewer's stray-tag claim was grepped and found not to exist.
- **v0.68.103** — First `/kbg:review-fixtures claude-md-health` loop — no standing critique memory, no substantive prior fix history. Since this skill's core mechanic is running real `git log`/`git branch`/`grep` against actual file history, 3 fixtures were built as real isolated synthetic git repos with controlled commit dates (not prose-only) to genuinely exercise it: a doc-vs-doc staleness case, a doc-vs-reality drift case (a branch that never existed, a flag-count claim that drifted), and the skill's own named failure mode (a self-contained doc with nothing checkable, testing whether "Test 3 not exercised" gets reported honestly). All 3 core mechanics held cleanly — both reviewers independently re-ran every cited `git`/`grep` command against the real fixtures and found no fabricated evidence anywhere. One real, grep-verified gap surfaced: Phase 4's verdict-column instruction was a loose example, not a pinned literal-string requirement, and the 3 dry runs each formatted it differently (title-case, inconsistent even within one report; invented qualifier states like "fail (partial)" in another) — fixed by pinning exact lowercase/hyphenated strings, re-verified live. One double-confirmed but non-target-attributable finding (a dry run misreported its target file's line count while quoting content) logged, not shipped — nothing in the doc instructs citing a line count, so it traces to no content gap. A genuine point *for* the skill surfaced unprompted: the freeform baseline's own summary got a date-math conclusion backwards in exactly the drift-during-summarization the skill's citation-per-cell table structurally prevents.
- **v0.68.102** — First `/kbg:review-fixtures task-prep` loop, deliberately scoped past a standing "don't re-audit without new evidence" memory (`task-prep-critique-v0618`, 4 converged passes) since `v0.66.0` added a genuinely new, unreviewed capability since that critique — Step 3.5's non-Jira `requirement-analyst` dispatch path. No `task-prep-workspace/` existed (gitignored), so this session built 3 fresh fixtures targeting that path, ran real sub-agent dispatches rather than simulating them. Both reviewers converged on a real regression: Step 3.5's exclusion list named "dependency version" as a mechanical-change example, and a live run read a SendGrid v2→v3 migration (different auth, payload shape, error semantics) as covered by it, skipping the requirement-analyst dispatch that would have surfaced the migration's real template/payload risk (independently proven missing by the without_skill baseline, which caught it unaided). Fixed by clarifying the exclusion covers a routine bump with no behavior change, not a migration changing what the caller adapts to; re-verified live, Step 3.5 now fires correctly. Second fix, single-sourced but grep-verified against `agents/requirement-analyst.md`: its `riskiest_assumption` field was never captured by Step 3.5 or prioritized at Step 6, so it competed anonymously in the generic `open_questions` list and could lose the last ask-slot — fixed by adding it to the capture list with its own priority tier; confirmed working in the same re-run, and as a side effect the ≤4-field overflow-priority rule (a third fixture's target) finally got exercised for real. One hypothetical, single-sourced finding (a compound draft blending a sourced change with a discretionary add-on could slip past Step 2's trivial-diff gate) logged, not shipped, per Rule 2.
- **v0.68.101** — `/kbg:review-fixtures learn` (drill-down): verified the 4 findings iteration-1 shipped as `de0e631` (v0.68.47) actually landed in the live `skills/learn/SKILL.md`, then closed the real gap that fix left open — no fresh run had ever re-measured whether the fix changed behavior. Re-ran all 3 iteration-1 fixtures against the current SKILL.md into a new `iteration-2/`, dispatched 2 independent reviewers comparing NEW vs OLD outputs. Both independently converged on a real regression: v0.68.47's "tool/platform default already enforces it" dedupe-exclusion clause was broad enough to fire on advisory prose (a system-prompt bullet, not a deterministic mechanism) and never required the cited default to be scoped to the transcript's own project — causing the negative-discipline fixture's single clearest candidate (a force-push rule with an incident-specific why) to be dropped entirely, a zero-candidate output on the exact assertion built to test it. Fixed by narrowing the clause to deterministic/fail-closed defaults that cover the candidate's full substance and are project-scoped; re-ran the fixture live against the new wording and confirmed the candidate now survives. One single-sourced, not-shipped finding (fix (a)'s store-derivation applied inconsistently across two fictional-project fixtures) matched an already-established, intentionally-declined precedent from iteration-1's own findings.md — logged, no change.
- **v0.68.100** — Committed `scripts/workflows/deep-research.js`, the canonical deep-research Workflow script consolidated this session from 8 ad-hoc inline runs (2026-07-03 through 2026-07-18) that had each re-authored the same ~23KB script from scratch instead of saving it once. Design: Scope → pipeline(Search → dedup → Fetch, no barrier) → barrier → 3-vote adversarial Verify → Synthesize, with hardcoded caps (`MAX_FETCH=15`, `MAX_VERIFY_CLAIMS=25`, `VOTES_PER_CLAIM=3`) instead of leaving fan-out size to the model — max agent count works out to ~98-99, matching both live runs exactly. Validated with two live runs on the same real question (Zig's release status) before committing: run 1 hung (98/99 sub-agents returned, 1 never completed, `status: running` for ~6h with a dead journal — most likely a stuck agent blocking the Verify `parallel()` barrier, though session-compaction-related orphaning couldn't be ruled out), stopped via `TaskStop`, no code change made on a single occurrence (Rule 2); run 2, same script unmodified, completed clean — 99/99 done, 0 errors, real cited report produced, 3 stale GitHub-cached claims correctly killed by adversarial verification — confirming the stall was very likely transient.
- **v0.68.99** — Added an mtime-based staleness signal to `memory-lint`, the one piece worth porting from a direct comparison against ECC's `continuous-learning-v2` instinct system. `advisor()` first flagged, then a follow-up read of ECC's actual source (`observer.md`, `instinct-cli.py`) confirmed: its "user didn't correct" confidence signal is a background Haiku agent inferring non-objection from silence in a raw tool-call log, with no approve/confirm/reject command anywhere in the CLI — the "never looked at" failure mode, not "survived N corrections" as first assumed, so the confidence/auto-apply layer stays unported. The one piece with a proven gap on kbg's side was ECC's decay formula (−0.02/week unobserved) — mirrored as a `memory-lint.py` check flagging entries untouched past `--stale-days` (default 90), printed as its own advisory section and deliberately not counted toward the exit code (an old memory isn't a defect the way a dangling link is). No schema change, no migration of the ~110 existing memory files — mtime was the only signal available without one. Verified live: 60 files flag at a 30-day threshold, exit code held at 62 regardless of `--stale-days`.
- Older releases (v0.68.27–v0.68.98): a known-gap callout for the shared "70" pass-threshold constant filed in-file per user request (v0.68.98, `commands/ship-merge.md`, documentation only, no behavior change — see `CHANGELOG.md` for the full entry); a `/kbg:compliance-audit` first-run pass (v0.68.97) on `ship-merge.md`'s automation-bias guard found and fixed two further bypasses in the just-shipped weight-≥15 invariant (generalized to the live ratio `D≥(7/3)×C`, then closed a second bypass via an explicit dual-recompute instruction), with a fourth, different-mechanism finding (the shared "70" pass-threshold constant) logged as an out-of-family known-gap per Rule 2; a review depth/level system for `kbg:review-pr` was investigated and rejected (Rule 2, v0.68.96) — the target population (single-file, non-test diffs) is 5.7% and already runs at the cost floor — but investigating *why* the need felt unmet surfaced a real bug instead: PR #2603 had invoked the Rule-2 trivial-diff economy to justify dropping `code-reviewer` for `typescript-reviewer` alone, a Phase 3 violation by substitution; fixed the substitution path plus a previously-unstated weight-≥15 invariant in `ship-merge.md`'s automation-bias guard; fixed a dangling cross-reference in `commands/review-fixtures.md` (v0.68.95, caught by a drill-down audit of the v0.68.94 fix itself, not a fixture loop) — Step 6 pointed back to "Step 0 above" but the file had no `### 0.` heading, only an unnumbered intro paragraph; fixed by promoting that paragraph into a real `### 0.` step; fixed `/review-fixtures` itself (v0.68.94) after surveying open gaps from everything learned running it — Step 6 now requires reviewer-dispatch eval prompts be quoted **verbatim**, never paraphrased (the v0.68.93 prompt-compression finding), and a new Step 3.5 reads an existing `feedback.json` before dispatching fresh reviewers instead of silently re-doing or skipping the work; also institutionalized 3 fixture-construction lessons (answer-key contamination, bench-script import-not-duplicate, trap file-locality) into `docs/reference/skill-fixture-review-prompt-template.md`; a `/review-fixtures`-style loop for `skills/score-decision/SKILL.md` (v0.68.93, first-ever for this target) found a headline "evidence-laundering" finding across both reviewers that did NOT survive verification (traced to a shared blind spot in the compressed `dispatch-prompts.md` paraphrase, not a real doc gap) — but two real, double-confirmed findings did survive, closing a Step 3 secondhand-evidence ambiguity and a Ranking-format auditability gap for a disqualified option with the highest raw weighted sum; First-ever `/review-fixtures`-style loop for `skills/recursive-improve/SKILL.md` (v0.68.92, no prior workspace existed) — real scripts/mutations meant `advisor()` was consulted on fixture safety pre-dispatch; one eval-1 baseline still ran real scripts against no explicit "don't touch real files" instruction and edited the real, already-shipped `skills/decide/SKILL.md` on disk, caught and reverted immediately, eval rebuilt with fictional paths; 2 independent reviewers found and fixed the strongest, double-confirmed issue — a fabricated "executed" witness-diff comparison that never ran — plus a headless-wait wording fix and a template gap for the unanswered-gate branch; a `/review-fixtures`-style loop for `skills/decide/SKILL.md` (v0.68.91, first-ever for this target) found and fixed a rungs-1–2 proportionality contradiction between the v0.68.32 fix and the `decide` completion criterion/Guardrails, a bundled-multi-row gap, and a bias-guard-ordering issue; a deeper re-verification pass on `agents/code-architect.md` (v0.68.90, blind-spot-hunter v0.68.87 precedent) re-ran `with_agent` fresh against the post-v0.68.86-fix doc for all 3 original evals — confirmed all 3 v0.68.86 fixes closed against actual shipped code, and fixed a regression neither original fix targeted (a domain→infra anti-pattern via an unqualified "closest analog is ground truth" bullet, fixed with an explicit precedence exception); a `/review-fixtures ship` pass (v0.68.89) on `commands/ship/COMMAND.md` found Phase 8 has no PR fallback for the ordinary first-push Path B case (its only action, "Invoke `/ship-merge`," hard-requires a PR) plus a GREEN-branch "no PR yet" note pointed at the wrong phase — fixed both, plus a tool-calling-vs-string-handoff wording nit; applied both target-attributable fixes iteration 3 of `build-error-resolver-workspace` had left pending (v0.68.88) — reworded the Recovery Strategies "Type mismatch" row to name the actual decision criterion (correct whichever declaration doesn't match the surrounding code's stated invariant, not "whichever type is narrower," which misdirected toward a masking trap), and added a Step 5 report-template requirement to note rejected alternatives that would also have built clean; also closed a separate iteration-4 gap via a new fixture where the correct fix unavoidably surfaces a second real error — no doctrine gap there, just a narrowed measurement claim; a session closing a gap in its own review-fixtures practice (v0.68.87) found `blind-spot-hunter`'s v0.68.43 fix had never been checked by an independent 2-agent pass or at the output level — re-ran 4 `with_agent` dry-runs fresh, confirmed all 3 targeted gaps closed, and fixed one real regression neither original review anticipated (a fresh run executed a mutation-test script in scratch space via a relocate-outside-the-repo loophole the doc's "must not run these" rule didn't cover); a `/review-fixtures agents/code-architect.md` loop (v0.68.86) found 3 doctrine gaps via 3 fixtures targeting layer-direction detection, premature-abstraction bait, and a load-bearing ambiguous requirement — fixed via a test-suite check, a code slot in the Output Format, and guidance for surfacing ambiguous requirements prominently; a first fixture-driven loop for `agents/backend-architect.md` (v0.68.85, no prior workspace existed) built 3 realistic 2-file fixtures and ran with-agent vs. baseline (`general-purpose`, no doctrine) — baseline independently caught a concurrency bug (agent 2 spun up a live Postgres container and proved with_agent's proposed atomic-claim SQL doesn't close the race, guard column ≠ mutated column) and a client-controlled-pricing bug that with_agent's own checklist missed on the same line; fixed via a worked-example callout on the guard-column requirement and a new "Source of truth" checklist row + matching Anti-Patterns row; a `summarizer` agent loop (v0.68.84, first use since v0.68.83's dispatch-prompts fix) found `iteration-1`'s fixtures predated v0.68.41's fix, so `advisor()` redirected the dispatch to inline the current doc for both agents and check whether v0.68.41's own Prose/Bullets/Table fork left a contradiction the 3 prior solo convergence rounds never checked for — both agents independently confirmed it had (Table's criterion structurally matched the Anti-Patterns causal-chain example without excluding it, self-contradictory OR/AND wording, a citation gap in the multi-throughline Output Format), all fixed and self-verified via whole-file re-read; a dispatch-prompts persistence fix (v0.68.83) for a gap confirmed twice — `/review-fixtures` composed dispatch prompts live in-conversation and never saved them, so the literal text reviewers received became unrecoverable once the session compacted; fixed in `commands/review-fixtures.md` Step 6 by writing both composed prompts verbatim to `<iteration-path>/dispatch-prompts.md` before dispatching (this fix's own file-persistence discipline is what later made the v0.68.93 prompt-fidelity correction possible to verify); a `ship-merge-workspace` pass (v0.68.82) closing 2 deferred fixture gaps (CI-partial-pending, missing-review-state) with 3 grep-verified doc fixes; a `cost-tracker.sh` fix (v0.68.81) that stopped attributing 100% of a mixed-model session's cost to whichever model was active at the last stop — grouped assistant turns by `.message.model` instead of tagging one whole-session sum, verified against a real 3-model transcript and regression-gated byte-identical against production data; a chased-and-initially-wrong multi-model cost-attribution bug (v0.68.80 — a plausible-looking "fix" would have double/triple-counted tokens, caught by reading the actual writer hook and a telescoping-sum identity proving the original total was correct all along; the real, narrower bug documented as a known limitation instead); a real UTC-vs-local timezone bug in `cost-report.md`'s "today"/"yesterday" buckets (v0.68.79, reproduced live — unfixed report showed "today: $972.84" when the real local-today total was $376), picked as a `/review-fixtures` target because it has real local production data (`costs.jsonl`) to mine directly; a fleet-count drift fix (v0.68.78, `plugin.json`/`marketplace.json`/`README.md` all claimed "18 commands" against a real 16 — same class of drift v0.68.72 fixed for the agent count); a `review-pr`-workspace pass (v0.68.77) mined 105 real production state files directly instead of dispatching fixtures first, finding the `rehunt` field held 15+ distinct shapes against 4 documented canonical values (20/105 missing it entirely) — fixed by treating a missing-or-non-canonical value the same as explicit `"incomplete"` in `ship-merge.md`'s Incomplete-review guard, closing the gap for both the session-that-wrote-it and the sessions-that-never-populated-it populations; a sustained `/review-fixtures` improve+optimize sweep across the fleet — build-error-resolver, refactor-cleaner, security-reviewer, backend-patterns/frontend-patterns builds and re-reviews, orchestrate, tech-humanize, task-prep, decide, ship-merge, address-review, silent-failure-hunter, requirement-analyst, typescript-reviewer, summarizer, blind-spot-hunter, code-implementer (×2), code-reviewer, typescript-patterns, python-reviewer, performance-optimizer, plan-reviewer (2nd reconciliation pass, v0.68.75, surfaced the read-first gap fixed by v0.68.83 above), and nextjs-reviewer (a factual/scoping-bug pass in v0.68.73 — 4 fixes, including a `proxy.ts` scoping gap that would've silently skipped the file its own Middleware guidance covers — then a first full with-baseline 2-reviewer eval loop in v0.68.74, which found the specialist's real edge was severity calibration, not raw bug-catching, and fixed a backwards `.bind()` Server Action encryption claim), plus the `/review-fixtures` command's own workspace-schema generalization (v0.68.62/.63) and self-check (v0.68.61). Each loop found and fixed 1-5 real bugs, verified against fixtures or live tools (Postgres, Node, `tsc --strict`). Also: `flutter-reviewer` removal (v0.68.72) and two Tathep framework-pattern removal batches (`adonisjs-patterns`/`dart-flutter-patterns`/`effect-ts-patterns`/`fastapi-patterns`/`hono-patterns`/`langchain-langgraph-patterns`/`tauri-v2-patterns` in v0.68.51, then `codebase-onboarding` in v0.68.52). See `CHANGELOG.md` for the full per-release record.
