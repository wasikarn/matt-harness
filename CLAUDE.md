# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Validation (run before committing)

```bash
claude plugin validate . --strict
```

Plugin manifest is the primary validation gate. `scripts/run-gauntlet.sh` runs plugin-validate + full shell-lint + JSON lint + harness-audit + the hook behavioral suite (`hooks/tests/test-gates.sh` + `test-flow-nudge.sh` + `test-session-stop.sh` — deny-gate + advisory-sensor + session/stop-hook unit tests) in parallel. The broader fleet critical-hooks suite and the eval dataset gate are pending rebuild.

## Adding or removing a surface

Auto-discovered directories: `agents/`, `skills/`, `commands/`, `hooks/`, `output-styles/`, `themes/`. See `skills/add-surface/SKILL.md` for the step-by-step (manifest bump, validation, BOUNDARY.md regen).

## Git hooks

Hooks live in `git-hooks/` (not `.git/hooks/`). Wire once per clone:

```bash
git config core.hooksPath git-hooks
```

pre-commit: fast gate — syntax/lint (`bash -n` + shellcheck), JSON validation, CRITICAL harness-audit (graceful-skip if absent).
pre-push: full gauntlet (all validation layers in parallel).

## Composer-not-creator doctrine

Before writing a new skill, command, or agent from scratch, check sources in this order: **(1)**
`mattpocock/skills` first — `claude plugin list` / the Skill tool's own available-skills list for
what's already installed under the `mattpocock-skills` plugin, plus the local clone at
`/Users/kobig/Codes/Personals/mattpocock-skills` for what's upstream-available but not yet
installed. This is a **Matt-Pocock-first harness** (his skills installed as the
`mattpocock-skills@mattpocock` plugin, namespaced `mattpocock-skills:<name>` — see README.md Quick
Start; migrated off the earlier unnamespaced `gh skill` installs 2026-07-17); checking
ECC/superpowers before matt's own repo gets the priority backwards. **(2)** the upstream ECC repo
at `/Users/kobig/Codes/Personals/ECC` and
the vendored superpowers checkout at `/Users/kobig/Codes/Personals/superpowers`. **(3)** sibling
harnesses under `/Users/kobig/Codes/Personals/` worth a structural-pattern check even when they're
not kbg's primary composer sources (e.g. `oh-my-claudecode` — cherry-picked before, ask if
unsure which repos currently qualify). Cherry-pick and adapt from whichever source fits; create
kbg-native surfaces only when none do. Confirmed gap (2026-07-17): `code-implementer`/`/implement`
were built checking only (2), skipping (1) — collided with matt's own `engineering/implement`
skill, caught by the user, not by this checklist. (A hand-pinned HEAD hash is structurally doomed
to re-stale; the path is the stable anchor — run `git rev-parse HEAD` there when you need the
current commit.)

## Architecture

The plugin ships as `kbg@kobig` from the `wasikarn/kbg-harness` GitHub repo. Claude Code loads all surfaces from `~/.claude/plugins/cache/kobig/kbg/<version>/` at startup. Nothing is symlinked.

**Doctrine injection:** `hooks/session/doctrine-bootstrap.sh` fires on SessionStart and injects `docs/METHODOLOGY.md` (decision-sizing triad + reasoning scaffold) into session context via `$CLAUDE_PLUGIN_ROOT` (the plugin install dir; the older `$CLAUDE_PLUGIN_DIR` name is not a real CC variable and expands empty).

**Operating model:** deny the irrecoverable set computationally (gates in `hooks/gates/`), advise on the rest (sensors in `hooks/advisory/`). Advisory sensors never emit `permissionDecision`. The L2–L5 autonomy ladder is retired.

**Why — the unifying crux:** the gate is a *verifier* (deterministic shell returning a branchable **score**), the model is the *maker*, and the maker can never grade its own work — an LLM judging its own output is circular ("two optimists agreeing"). So advisory sensors journal but never gate, and the autonomy ladder had to retire: a model-as-gate is the maker appointing its own verifier. **Score, not feel** — every loop's stop condition must be a number a deterministic gate can branch on, never a vibe the model rationalizes. (This is the agent-loop verifier-separation principle; see `docs/research/` + the retired L2–L5 build for the proven failure it prevents.)

When hooks are wired: gates/ (deny), advisory/ (journal), session/ (inject), stop/ (cost tracking).

## Skill authoring doctrine (matt-pocock)

When creating or editing a skill under `skills/`, follow matt-pocock's `writing-great-skills` doctrine — canonical: the `mattpocock-skills:writing-great-skills` skill (installed as the `mattpocock-skills` plugin, not vendored in this repo since v0.46.0 — see README.md Quick Start; leading word, ≤25-word description, completion criterion, no-op test, two-cuts, failure-mode guard).

The `docs/skill-template/SKILL.md` template carries this checklist as a `## Design checks` section — but `harness-audit` check 36 does **not** check for that heading's presence. It checks 4 of the 6 doctrine elements via INFO-only regex proxies against each skill's live description/body (leading-word vocabulary, ≤25-word count, completion-criterion phrasing, a no-op-test line-count heuristic); "two-cuts" and "failure-mode guard" have no shell check — a failure-mode regex proxy was tried and retired 2026-07-16 (vacuous before a reset-bug fix, 5/5 false-positive after: every flagged skill already named its failure mode in a prose section or bullet list the numbered-window proxy couldn't see). INFO findings never fail the gate. Confirmed 2026-07-22: only 2/35 native skills (`pr`, `task-prep`) actually carry a `## Design checks` section — the template's checklist is documentation, not an enforced requirement.

**Named Model footers:** a skill/command/agent that makes load-bearing reasoning/judgment choices may end with a `## Named Model` footer citing cc-thinking-skills lenses. Apply the 3-condition rubric from `memory/mental-models-sweep-v0302-2026-07-03.md`: (1) load-bearing reasoning gap, (2) name-a-lens benefit for the operator, (3) honesty posture preserved (footer is a scaffold + catalog pointer, never "this lens proves correctness"). The curated catalog is `docs/reference/reasoning-models.md`; the 39 raw models live under `docs/reference/thinking-skills/skills/`.

**Suggested next step footers:** a workflow surface (command or workflow skill run as a discrete step) may end its Output/Summary phase with a `Suggested next step:` marker — outcome-branched (`situation → action`), citing skills as `kbg:<name>` and commands as `/<name>`. Skills are ALWAYS cited `kbg:`-form (never `/name`) — get this right at authoring time: `harness-audit` check 40 only catches rename/deletion drift on refs already in `kbg:` form, it does **not** scan for a skill mis-cited in slash form (confirmed: this exact bug shipped twice — `commands/pr.md` and `diagnosing-bugs/SKILL.md` both cited a skill as `/name` undetected until a manual survey caught it, v0.35.0). Passive suggestion only — never "invoke X now" / auto-chain (that collides with the no-model-self-start doctrine). Skip self-contained reference/pattern/catalog surfaces (a forced footer there is the retired canonical-sections ceremony, 2026-06-16) and terminal workflows (post-mortem, ship-release terminus).

**Escalation to `AskUserQuestion`:** a branch belongs in the passive footer only while it's anticipatory — conditional on a fact not yet known (did the reviewer comment, did CI go red). If every branch is already true/decidable right now and there's no sensible default, that's a present-tense fork, not a suggestion — surface it via `AskUserQuestion` (per `output-styles/staff-eng.md`'s decision-question rule: one-line consequence per option) instead of text the user might not read. Model: obra/superpowers' `finishing-a-development-branch` skill, which ends by presenting exactly N concrete options (merge/PR/keep/discard) and blocking for the pick — not superpowers' separate (and rejected) `using-superpowers` auto-chain directive. None of kbg's shipped footers (v0.35.0/.1) currently qualify — they're all anticipatory-conditional — so this is a criterion for future surfaces, not a rewrite of what shipped.

## Branching model

Single branch: `develop` only. No feature branches. Commit and push direct.

**Computationally enforced** by `gate:worktree:develop-only` (`WorktreeCreate` event) and the `git worktree add -b` block in `gate:bash:irrecoverable` (`PreToolUse:Bash`). Both gates are opt-in per repo via the `/.kbg-no-worktree` sentinel — present in the kbg-harness repo, absent from tathep/ECC/scratch repos (which keep their existing `gate:write:worktree-guard` redirect). Detached `review-pr-<N>` worktrees in `$TMPDIR` are explicitly allowlisted so the Phase 2 PR-by-number review path keeps working.

## Non-obvious gotchas

Grouped by behavior area (not a flat bucket — find the group first, then the line).

### Repo & commit hygiene

- **Hardcoded home paths blocked:** `.sh`/`.py` files must use `$HOME` or `~`, never `/Users/<name>`. The pre-commit gate will reject the commit.
- **Never `rm -rf`:** use `trash` for deletions.
- **Never `--no-verify`** on commits or pushes.
- **Stage by name:** never `git add -A` or `git add .`.

### Plugin lifecycle & install

- **`defaultEnabled: false`:** plugin ships disabled. After install, add `"kbg@kobig": true` to Claude Code `settings.json`, then restart.
- **Cache-invalidation:** same-version edits are no-ops. Always bump both manifests before `claude plugin update`. CLAUDE.md-only edits skip the bump — it's dev-facing repo guidance, not cached plugin content (only `agents/`/`skills/`/`commands/`/`hooks/`/`output-styles/`/`themes/` ship per-version).
- **Re-verifying a same-session skill edit:** don't hand a fixture-loop re-verification agent a `Skill(<name>)` invocation to test an edit that hasn't been bumped/reinstalled yet — it resolves against the installed cache (`~/.claude/plugins/cache/kobig/kbg/<version>/`), not the live repo file, and silently tests stale content with no error. Confirmed 2026-07-27 (tech-humanize v0.68.59): one re-verification agent got a false "fix confirmed" this way; instruct it to `Read` the repo path directly instead.
- **`BOUNDARY.md` regen:** the script writes to STDOUT, not the file. The `> BOUNDARY.md` redirect is required every time.
- **Output style:** `output-styles/staff-eng.md` is the sole live-response register — self-calibrates terse vs full decision-framing by stakes, not by switching files (the old `senior-eng.md`/`staff-eng.md` two-file split was collapsed 2026-07-02; the internal "Calibrate to stakes" rule replaces the escalation/fallback dance). `force-for-plugin: true` auto-activates it whenever `kbg@kobig` is enabled, overriding the user's own `outputStyle` setting — no `/output-style` selection needed, but it also means you can't run a different style while this plugin is on without disabling it first.

### Session environment quirks

- **Working frames:** `contexts/` holds `dev.md`, `review.md`, `research.md` — loaded by `/frame` to set session posture.
- **`grep` is aliased** to `rtk grep` in this environment. Use `/usr/bin/grep` or `awk` for count/stat operations.

### Skill/agent/command mechanics & routing

- **Skill descriptions load on every Task spawn** (~words×1.3 tokens). Keep descriptions ≤25 words.
- **Thinking models:** default is the triad + `advisor()` inline (METHODOLOGY Rule 1) — `kbg:decide` is on-demand only, for genuinely hard/contested-diagnosis choices (de-scoped 2026-07-02, v0.21.4: 0 real-world invocations vs 55 `advisor()` calls across 182 sessions). The 39 on-demand mental-model files live in `docs/reference/thinking-skills/skills/` (never move to `skills/` — would break fleet count).
- **`disable-model-invocation: true`:** carried by 2 skills currently — `recursive-improve` and `score-decision` (re-check via `grep -rl "disable-model-invocation: true" skills/*/SKILL.md`; the count drifted hard after v0.46.0 moved 17 flag-carrying matt-origin skills out of this repo, and nobody updated this line). Both are now **CRIT**-guarded against the flag being silently dropped: `recursive-improve/SKILL.md` by check 39, `score-decision/SKILL.md` by check 49 (added 2026-07-23 after a `kbg:plan-reviewer` pass on a skill-improvement batch plan flagged that skill-creator's own description-optimizer rewrites SKILL.md frontmatter, and only recursive-improve had a real gate). Check 30 still only WARNs that a `-reason` field exists on whichever skills currently carry the flag — it's the presence-of-reason check, not the flag-survives-a-rewrite check; the two CRIT checks above are what actually close that gap now.
- **`review_mode` in `ship-merge`:** `review-pr` tags its state write `pr-by-number` (isolated worktree) or `own-branch` (self-review). `ship-merge` caps the Critical-findings score at the fatal-weakness floor on sensitive-path diffs reviewed `own-branch` — an automation-bias guard against trusting a same-session self-review's severity tiering. "Sensitive-path" covers both auth/secret/credential/payment/billing/token AND the harness's own verifier/gate code (`hooks/gates/**`, `hooks/hooks.json`, `skills/harness-audit/scripts/{audit.sh,checks/**}` — the same list `hooks/gates/verifier-protect.sh` protects).
- **`orchestrate` vs `kbg:decide`:** orchestrate decides whether/how to spend effort on an ask (inline/parallel/sequential/drop, which surface receives it) *before* it's understood as a bounded decision; `decide` reasons through a bounded question once you're already committed to answering it. A pile of competing asks routes through `orchestrate` first; a single reversible-choice question goes straight to `decide`.
- **`SKILL.md` frontmatter ≠ `agents/*.md` frontmatter:** the real skill-file field for tool control is `allowed-tools` (pre-approves without asking; there's no hard-restriction field for skills), not `tools:` — that's the `agents/*.md` subagent field (hard-restricts to a fixed set). Confirmed against the official 16-field `skills.md` reference and the shipped CLI binary's own compiled schema key list. `metadata` / `metadata.origin` / `disable-model-invocation-reason` are non-standard-but-harmless kbg conventions — Claude Code tolerates unrecognized frontmatter keys (confirmed via changelog + `plugin.json`'s own documented "unrecognized fields → warning, not error" policy), they just carry zero behavioral effect.
- **`/goal` vs `goal-craft`:** `/goal` is Claude Code's own native completion-condition loop (v2.1.139+, session-scoped, judged each turn by a separate small model that reads only the transcript). kbg never wraps or auto-invokes `/goal` itself — the user always types `/goal` themselves, no exceptions. `skills/goal-craft/SKILL.md` only composes a paste-ready condition string (mandatory one-way-door screen + turn bound) and, as of 2026-07-08, is model-invocable (its `disable-model-invocation` flag was removed on user request) — the model may draft a condition unprompted, but the string is inert until the user pastes it after `/goal`. Auto-dispatch of `/goal` itself (`claude -p "/goal ..."`) is still a deliberate non-goal — it forks a separate headless session and reopens the retired L4/L5 "no model self-start" invariant.

## Recent versions

Quick orientation for the last few releases. For full notes see `CHANGELOG.md`.

Cap this list at 10 bullets. Keep the last 3-5 releases as individual entries; fold older ones into version-range bullets (`vX.Y.a–vX.Y.b` or `vX.Y.x`) by theme. Adding an 11th bullet means dropping the oldest — `CHANGELOG.md` is the full record, this section is orientation only.

- **v0.68.60** — User asked whether prior `/review-fixtures` loops (backend-patterns, frontend-patterns, orchestrate) had every confirmed finding actually fixed, same as tech-humanize just did. Audited all 3 `feedback.json` files against current SKILL.md rather than trusting past CHANGELOG claims: backend-patterns/frontend-patterns clean, but `orchestrate` had 1 of 3 confirmed findings still open — an item routed to a Skill (`mattpocock-skills:*`) structurally escaped the gating classification entirely, since Step 4's Ungated/Gated lists only enumerate Agent-tool dispatches by name and a Skill has no hard `tools:` ceiling of its own (its `allowed-tools` field only pre-approves calls, it doesn't restrict the invoking actor). Fixed: gate on the actor invoking the Skill, not the Skill's name — Gated by default when that's the lead session itself. Re-verified live (reading the repo file directly, not `Skill(kbg:orchestrate)`, per the stale-cache gotcha just documented in v0.68.59): 3 Skill-routed items all correctly classified Gated, no fleet escape-hatch agent clears the read-only bar. Verification agent flagged its own evidence's scope limit honestly: the eval hinted at the skill name, so this proves the classification fires correctly once a Skill route exists, not that a lead spontaneously prefers a Skill unprompted.
- **v0.68.59** — First-ever `/review-fixtures` run on `skills/tech-humanize/SKILL.md` (3-eval EN/TH set, 2 independent staff-eng agents). Found 3 real skill-attributable bugs, all fixed and live-reverified: SKILL.md's own worked calque-fix example fabricated a number ("4 เคส", also miscategorized — not a calque, an anti-fabrication case; traced to an ambiguous placeholder in `patterns-thai.md` §32, now marked `[N]`); the Grit Gate's fabrication-boundary escape hatch only covered whole-source hollowness, not a single unverifiable claim inside an otherwise-real draft (added a Tier-2 hedge rule); and the "zero em dashes" rule wasn't mechanically enforced (a with_skill run shipped 2 em dashes while its own change summary claimed none — worse than the unguided baseline's 1 em dash on the same prompt). Live re-verification caught its own methodology trap: the first em-dash re-check silently tested the stale installed plugin cache (byte-identical to pre-fix repo content) via `Skill(kbg:tech-humanize)` instead of the live edit, giving a false "confirmed" — caught via a second agent's independent self-correction on the same trap, then fixed by re-reading the repo file directly. Both fixes held once genuinely tested against the live edit. New CLAUDE.md gotcha added so a future fixture-loop re-verification doesn't repeat it.
- **v0.68.58** — Ran a 2-eval `skill-creator` fixture loop against `orchestrate` (a degenerate-urgency backlog with a Fast-Path-eligible item, and an 8-item pile testing the fan-out cap + no-live-user headless gate) via `/review-fixtures orchestrate`, per user request to build fixtures for that new command. 2 independent staff-eng agents reviewed all 4 outputs; reconciled findings confirmed the v0.68.28 Fast Path Gate and Validator-carve-out fixes both hold under fresh adversarial scenarios (verified live against `agents/security-reviewer.md`/`agents/summarizer.md` tool grants), and surfaced 2 new skill-attributable bugs, both fixed: the Output Format's Route/Status column enum (`inline/parallel/sequential/drop`) didn't match the vocabulary the worked Example and `reference.md`'s Path column actually used — same defect class v0.68.28 closed once elsewhere, recurring; and the Example's "prod 500s" row labeled itself "sequential" while staffing only one agent, contradicting the skill's own Builder→Validator chain doctrine — fixed by adding the missing Validator leg (and 2 sibling rows that shared the enum bug) plus a bridging rule translating `reference.md`'s Path vocabulary down to the 4-value Route enum. 3 more findings (a wrong-matrix citation, a nonexistent-file rationale in a fixture, a security-reviewer scope overreach) were fixture-only or cosmetic, not skill defects — recorded in `feedback.json`, no further change.
- **v0.68.56** — User asked whether backend-patterns (v0.68.53/.54) had gone through the same 2-agent-Outputs-review-delegation pattern just established for frontend-patterns — it hadn't (that release used solo fresh-context whole-file re-review instead), so re-ran the pattern retroactively against `backend-patterns-workspace/iteration-1/`'s 6 existing outputs (no fixture re-run needed). Both agents converged on one real bug: the rate-limit-serverless with_skill output uses the raw API key as a literal Redis key, violating SKILL.md's own stated hash-the-key rule — while the no_skill baseline hashed it correctly with zero guidance, which `advisor()` flagged as real counter-evidence against "the rule wasn't sticky," so the fix (a PASS/FAIL code anchor added to the previously example-free Rate Limiting section, verified empirically against a real `createHash` call and `@upstash/ratelimit`'s installed source) is recorded as a hypothesis acted on, not a proven mechanism. 4 more real, non-overlapping bugs found in the other outputs don't trace to any SKILL.md example — recorded in `feedback.json` as fixture-only findings, no skill change.
- **v0.68.55** — Built `skills/frontend-patterns` (React/TS component/hook/state/forms/rendering-perf patterns) via `skill-creator:skill-creator` + a dispatch row in `code-implementer.md`. Per user request, delegated both the Eval Set Review step and — after a mid-turn pivot — the fixture Outputs review itself to 2 independent staff-eng agents each (a twice-reinforced delegation preference). Found and fixed a real bug in the skill's own canonical Virtualization example (fixed `estimateSize` with no `measureElement`, inherited verbatim by the with-skill fixture run and missed by its own review agent — only a post-hoc cross-check caught it) plus an unrelated missing `useRef` import in the same block; corrected `benchmark.json`'s "Honest headline" note, which had implied the example was fully clean; retracted my own over-praise of a scroll-reset that turned out to be dead code from component unmount/remount timing; verified 2 real (non-skill-attributable) date-validation bugs in the without-skill baseline. Skill count 29 → 30.
- **v0.68.54** — Follow-up to v0.68.53, same day: user asked to verify the fix actually works against a real query, then to check "the other two [fixes]" the same way. Live `Skill(kbg:backend-patterns)` invocation confirmed the loaded content matched the fix; a live Postgres run re-confirmed the transaction `market_id` fix against an adversarial stale-id input; live Node execution confirmed the sort-direction fix (old order put the unmatched market first, the best match last). The Middleware Pattern fix did not hold up: v0.68.53 credited it with fixing a `no-return-await`-inside-`try` bug, but 3 live Node runs proved the original code's `try/catch` could never have caught a handler's promise rejection either way (an async function never throws synchronously, so try-placement doesn't matter without `await`) — and adding `await` would've been the wrong fix anyway, since it would mislabel the handler's own bugs as "Invalid token." Kept the restructuring (it stops the code from implying protection it never had) but corrected the SKILL.md comment and the v0.68.53 entry above to not claim a behavior change that didn't happen. One of eight "confirmed and fixed" bugs from that release turned out to be a clarity improvement wearing a bug-fix's justification — caught by running the code instead of re-reading the reasoning that produced the fix.
- **v0.68.53** — `skill-creator:skill-creator` improve+optimize loop run against `skills/backend-patterns/SKILL.md`, per user request to delegate the eval-set-review step to 2 staff-eng agents. This ECC-origin file had never been fixture-tested before. Improve: 3 fixtures refuted the original hypothesis on both with-skill and no-skill outputs, but grading surfaced 2 real gaps anyway (Background Jobs welded a depth-cap fix to a durability fix that only applies to the architecture it replaces; Rate Limiting named the rejected pattern's failure mode but not the recommended one's, and 2 dry runs silently resolved that in opposite directions). What followed was 5 rounds of fresh-context whole-file adversarial re-review, each finding genuine new defects — some freshly-edited, some in untouched Repository/Service/Transaction/RBAC sections that had shipped unexamined since import. Every fix verified empirically: a live Postgres 16 container (3x), `tsc --strict` against a real TypeScript install (multiple times, real `TS2551`/`TS2339`/`TS2741`/`TS2591` errors), live Node execution tracing async interleaving. Confirmed and fixed: a Transaction Pattern SQL exception handler that silently swallowed failures as fake successes (`RETURN {success:false}` instead of re-raising — `supabase.rpc()` came back `error: null`); the same SQL never linking the new position to the market it had just created (verified live: a stale client-supplied `market_id` silently attached to the wrong pre-existing market); a missing `findByIds` on `MarketRepository`; `vectorSearch` with no return statement (6 real `tsc --strict` errors); RBAC's `User` type not matching what `requireAuth()` actually returns; a Middleware Pattern example that had drifted to Pages Router while the rest of the file is App Router — rewritten to match, though the bug this rewrite was credited with fixing turned out not to be real (see v0.68.54); `searchMarkets` sorting by similarity ascending instead of descending; and 3 scope-overclaim fixes (`/Express` and `or GraphQL` dropped from a file with zero content backing either, SQLAlchemy pool-sizing vocabulary corrected to Node terms). 2 findings assessed and deliberately kept as-is rather than silently dropped (a pre-existing missing try/catch on one RBAC path; a functionally-no-op `EXCEPTION` block kept for its pedagogical warning value). Optimize: drafted 24 items, 2 independent staff-eng reviewers found non-overlapping real gaps — reviewer 1 caught a factual overclaim in one item's note plus 4 named-but-untested boundaries (logging, indexing, the skill's own `kbg:security-auditor` hand-off, `kbg:backend-architect`'s architecture-vs-per-query N+1 distinction); reviewer 2 caught a systematic bias (8/12 false items had an obvious blocklist token, 9/12 true items had none — a keyword matcher would score 20/24 without evaluating real content-fit) and fixed it by adding token-free negatives rather than tech-naming the positives, plus 3 legitimate multi-skill co-dispatch cases moved to `manual_inspection_items`. Net 24 → 29 scored + 3 manual. Live run: 15/29 raw but cleanly split — 15/15 false items correct (one routing to the actually-right sibling), 0/14 true items (every true-item query narrates an ambient codebase that doesn't exist in the harness's actual repo, so the model investigates via Bash/ToolSearch rather than assuming a build task) — a measurement artifact, not a real under-trigger, since this skill's primary path is `code-implementer.md`'s deterministic stack table. No description change shipped, per Rule 2.
- **v0.68.52** — Removed `skills/codebase-onboarding` (ECC import) per user follow-up: after v0.68.51's 7-skill removal, asked which of the 6 remaining zero-invocation skills were also worth cutting. Checked actual usage against ~all session transcripts (7216 files, every project) plus each skill's own stated scope rather than trusting zero-invocation alone — 5 of 6 (`agent-architecture-audit`, `claude-md-health`, `context-budget`, `eval-harness`, `production-audit`) had a real reason to exist despite zero measured use (live target system, proven track record, explicit differentiation, or cross-references from other skills) and were kept. `codebase-onboarding` was the clean case: generic import, no fleet dependency (`spec-miner` — the one natural pairing — explicitly says it doesn't need it), trigger condition ("joining a new project") that structurally doesn't recur against this user's fixed repo set. Cleaned 2 dangling references in `agents/spec-miner.md` and one in `commands/ask-kbg.md`. Skill count 29 → 28.
- **v0.68.51** — Removed 7 Tathep-scoped framework-pattern skills (`adonisjs-patterns`, `dart-flutter-patterns`, `effect-ts-patterns`, `fastapi-patterns`, `hono-patterns`, `langchain-langgraph-patterns`, `tauri-v2-patterns`) per user request. Deleted the directories and swept every live-surface reference: README.md's Tathep Platform table plus 3 skill-count callouts (36→29), `agents/backend-architect.md`'s `*-patterns` citation lists, `agents/code-implementer.md`'s stack-detection table (multi-match example reworded off Hono), `agents/python-reviewer.md`'s now-vacuous FastAPI pointer (removed outright — no other kbg Python-framework skill to substitute), `commands/ask-kbg.md`'s framework-skill list, and 2 illustrative examples in `skills/inventory/SKILL.md`/`skills/typescript-patterns/SKILL.md`. Left `docs/plans/skill-improvement-batches.md` alone — a closed, dated historical record of already-completed work on those skills, same treatment as `CHANGELOG.md`'s own past entries. Skill count 36 → 29.
- **v0.68.50** — Built `skills/typescript-patterns` (TypeScript idioms + `tsconfig.json` choices, 5.9–7.x compatible) per a Thai-language user request, then ran a full `skill-creator:skill-creator` improve+optimize loop with the eval-set-review step delegated to 2 staff-eng agents. Self-caught a real research gap before it shipped: an initial narrow `npm view typescript versions` filter (only `6.` prefix) missed that TypeScript 7.0 was already GA (7.0.2, published 2026-07-08) — caught via a background agent's independent finding, every version claim then re-verified against real `5.9.3`/`6.0.3`/`7.0.2`/`tsgo` installs. Improve: 1 fixture confirmed a real gain (discriminated-union exhaustiveness guard), 2 turned out non-discriminating once `grade.sh` test-harness bugs were fixed — reported honestly. Optimize: reviewer verified a real, generalizable bug in the reused `detect_trigger.py` (`Skill` branch logged but never scored a trigger) and fixed it; live run (23/23 items) scored 15/23 raw but 11/11 on negatives (incl. 3 correct sibling-skill routes) and split exactly on implementation-vs-advisory for positives (4/5 vs 0/7) — matching skill-creator's documented triggering mechanism, no description change from that alone. `advisor()` then caught two more issues before commit: the write-up above had wrongly lumped this clean, asymmetric result in with the fleet's prior *non-discriminating* precedents (no signal at all) — corrected to state the finding on its own terms — and SKILL.md's own description/"When to Activate" still said "any `.ts`/`.tsx` file," contradicting the narrower `code-implementer` gate ("tsconfig or a type-modeling/compiler-option decision in scope") already narrowed earlier in the same session — the ninth confirmed instance of "fix ships a new contradiction" ([[re-review-after-every-fix-round-2026-07-25]]). Also closed harness-audit's own check-36 WARN/INFO findings on the new skill (negation clause, word-count trim, `typescript` added to the coined-term vocabulary list) rather than shipping with avoidable noise. Skill count 35 → 36.
Older releases (including v0.68.27's 7-skill optimize pass, v0.68.28's eval-driven `orchestrate` loop, v0.68.29's tech-humanize loop, v0.68.30's task-prep improve+optimize loop, v0.68.32's `decide` improve+optimize loop, v0.68.33's `probe`-mode follow-up, v0.68.34's `ship-merge` fixture-based improve+optimize loop, v0.68.35's `address-review` improve+optimize loop, v0.68.36's `silent-failure-hunter` improve+optimize loop (the fleet's first against an Agent rather than a Skill/command), v0.68.37's `requirement-analyst` improve+optimize loop (disproved its own fetch-boundary hypothesis via a corrected `detect_trigger.py`), v0.68.40's `typescript-reviewer` improve+optimize loop (1 real fix, stop-cold-vs-check-failed distinction), v0.68.41's `summarizer` improve+optimize loop (structure-selection gate wasn't wired into Output Format, caught live), v0.68.43's `blind-spot-hunter` improve+optimize loop (rebuilt fixtures as multi-file mini-repos to match the agent's real cross-file entry condition; caught a fabricated `advisor()` tool-call claim), v0.68.44's `code-implementer` improve+optimize loop (4 real fixes from 4 fixtures, 2 re-review rounds; near-zero eval-set discriminating power, not run live), v0.68.45's `code-reviewer` improve+optimize loop (5 real fixes from 4 git-backed fixtures across 3 re-review rounds; eval-set live run inconclusive since the headless harness produced zero Agent-tool dispatches, so the triggering hypothesis was never actually exercised), v0.68.46's `plan-reviewer.md` close-out (a one-pass consistency check on a file left uncommitted by 2 concurrent sessions, not a fixture-based loop), v0.68.47's `learn` improve+optimize loop (4 dedupe-check fixes; non-discriminating live eval traced to a cold headless session structurally lacking mineable multi-turn content, not a wording defect), v0.68.48's llm-wiki-article study (1 real fix: check 40's slash-form-citation gap had a live 3rd instance in `commands/post-mortem.md`, closed with check 50 as the sibling regression guard; rest confirmed already covered or declined with reasons stated), and v0.68.49's second `code-implementer` improve+optimize loop (5 real fixes from 3 new fixtures targeting angles v0.68.44 never touched; 2 re-review rounds; optimize step reproduced v0.68.44's non-discrimination result on 10 fresh queries, folded 5 new eval items in, no description change)): see `CHANGELOG.md` for the full record.
