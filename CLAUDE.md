# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Validation (run before committing)

```bash
claude plugin validate . --strict
```

Plugin manifest is the primary validation gate. `scripts/run-gauntlet.sh` runs plugin-validate + full shell-lint + JSON lint + harness-audit + the 8-file hook behavioral suite (`hooks/tests/test-gates.sh`, `test-worktree-guard.sh`, `test-flow-nudge.sh`, `test-jira-route-nudge.sh`, `test-session-stop.sh`, `test-learn-nudge.sh`, `test-plan-review-nudge.sh`, `skills/harness-audit/tests/test-harness-audit.sh` — deny-gate + advisory-sensor + session/stop-hook + harness-audit unit tests) in parallel. The old 204-test critical-hooks suite and eval dataset gate were deleted, not rebuilt, in the 2026-06-27 owner-authorized reset (`c452102`) — most of what they tested was the L3/L4/L5 autonomy machinery retired by ADR 0006, and current coverage (harness-audit's 52 checks + the suite above) already exceeds what they checked. Recovery anchor if ever wanted: `24d7663`.

## Adding or removing a surface

Auto-discovered directories: `agents/`, `skills/`, `commands/`, `hooks/`, `output-styles/`, `themes/`. See `skills/add-surface/SKILL.md` for the step-by-step (manifest bump, validation, BOUNDARY.md regen).

## Git hooks

Hooks live in `git-hooks/` (not `.git/hooks/`). Wire once per clone:

```bash
git config core.hooksPath git-hooks
```

pre-commit: fast gate — syntax/lint (`bash -n` + shellcheck), JSON validation, CRITICAL harness-audit (graceful-skip if absent).
pre-push: full gauntlet (all validation layers in parallel).

## Composer-not-creator doctrine

Before writing a new skill, command, or agent from scratch, check sources in this order: **(1)**
`mattpocock/skills` first — `claude plugin list` / the Skill tool's own available-skills list for
what's already installed under the `mattpocock-skills` plugin, plus the local clone at
`/Users/kobig/Codes/Personals/mattpocock-skills` for what's upstream-available but not yet
installed. This is a **Matt-Pocock-first harness** (his skills installed as the
`mattpocock-skills@mattpocock` plugin, namespaced `mattpocock-skills:<name>` — see README.md Quick
Start; migrated off the earlier unnamespaced `gh skill` installs 2026-07-17); checking
ECC/superpowers before matt's own repo gets the priority backwards. **(2)** the upstream ECC repo
at `/Users/kobig/Codes/Personals/ECC` and
the vendored superpowers checkout at `/Users/kobig/Codes/Personals/superpowers`. **(3)** sibling
harnesses under `/Users/kobig/Codes/Personals/` worth a structural-pattern check even when they're
not kbg's primary composer sources (e.g. `oh-my-claudecode` — cherry-picked before, ask if
unsure which repos currently qualify). Cherry-pick and adapt from whichever source fits; create
kbg-native surfaces only when none do. Confirmed gap (2026-07-17): `code-implementer`/`/implement`
were built checking only (2), skipping (1) — collided with matt's own `engineering/implement`
skill, caught by the user, not by this checklist. (A hand-pinned HEAD hash is structurally doomed
to re-stale; the path is the stable anchor — run `git rev-parse HEAD` there when you need the
current commit.)

## Research: check qmd before web search

Before starting primary-source research — fact-checking a claim, investigating a library/API,
verifying a citation — search the local `qmd` collections first, then query `context7` for any
library/framework doc lookup, before reaching for `WebSearch`. Relevant collections: `kbg-research`
(this repo's own `docs/research/`), `kbg-memory` (this repo's own memory store), `llm-wiki` (the
operator's personal knowledge vault, 1,600+ docs spanning every project), plus the tathep/dotfiles
collections. Run `qmd status` (or the `status` MCP tool) for the full current list; scope a query
to the relevant collections rather than searching all of them blind.

**Why this line lives here, not in a skill:** kbg built exactly this qmd-first behavior once
already, wired into a vendored `research` skill (v0.44.0, explicitly kept vendored so "a bare
install would lose" the qmd/context7 fold-in). It was deleted anyway — commit `d99ccf3` swapped it
for a bare `gh skill` copy, then v0.58.11 swapped that for the upstream `mattpocock-skills:
engineering/research` plugin skill, which carries no qmd or context7 awareness (confirmed by grep,
2026-07-30). Two unrelated migrations, both aimed at fixing namespace collisions, silently dropped
this value with nobody catching it. A skill file is exactly the kind of surface an upstream resync
can overwrite out from under you; `CLAUDE.md` isn't. The rule lives here so it survives the next
resync and applies to every research-shaped task, not just the ones that happen to route through
one particular skill.

**Cross-project reach (v0.68.106):** this file only loads when cwd is `kbg-harness` — a
foreign-project session never sees this rule. The operator's own `~/.claude/CLAUDE.md`
(dotfiles-owned, not shipped with this plugin) now carries a short mirror of the `llm-wiki`
half of this rule, so the vault stays reachable from any project. That mirror doesn't replace
this section — it's a thinner pointer for a context this file can't reach. `/kbg:wiki-ingest`
and `kbg:wiki-scan` (this repo) give the vault a user-invoked write path and a read-only health
check, respectively; see their own files for the doctrine reasoning (public-plugin-touching-
private-vault, graceful-skip when `~/llm-wiki` is absent, `disable-model-invocation` on the
mutating command).

**Live-fire confirmed (2026-07-30):** a fresh session in a genuinely different project (not
kbg-harness) spontaneously reached for `qmd` scoped to `collection: "llm-wiki"` on a research
question, unprompted — the mirror rule above reaches a foreign session as designed. This was
the one part of the design that couldn't be verified by grep or script (a prompt-only routing
rule either triggers in the wild or it doesn't); it does.

## Architecture

The plugin ships as `kbg@kobig` from the `wasikarn/kbg-harness` GitHub repo. Claude Code loads all surfaces from `~/.claude/plugins/cache/kobig/kbg/<version>/` at startup. Nothing is symlinked.

**Doctrine injection:** `hooks/session/doctrine-bootstrap.sh` fires on SessionStart and injects `docs/METHODOLOGY.md` (decision-sizing triad + reasoning scaffold) into session context via `$CLAUDE_PLUGIN_ROOT` (the plugin install dir; the older `$CLAUDE_PLUGIN_DIR` name is not a real CC variable and expands empty).

**Operating model:** deny the irrecoverable set computationally (gates in `hooks/gates/`), advise on the rest (sensors in `hooks/advisory/`). Advisory sensors never emit `permissionDecision`. The L2–L5 autonomy ladder is retired.

**Why — the unifying crux:** the gate is a *verifier* (deterministic shell returning a branchable **score**), the model is the *maker*, and the maker can never grade its own work — an LLM judging its own output is circular ("two optimists agreeing"). So advisory sensors journal but never gate, and the autonomy ladder had to retire: a model-as-gate is the maker appointing its own verifier. **Score, not feel** — every loop's stop condition must be a number a deterministic gate can branch on, never a vibe the model rationalizes. (This is the agent-loop verifier-separation principle; see `docs/research/` + the retired L2–L5 build for the proven failure it prevents.)

When hooks are wired: gates/ (deny), advisory/ (journal), session/ (inject), stop/ (cost tracking).

## Skill authoring doctrine (matt-pocock)

When creating or editing a skill under `skills/`, follow matt-pocock's `writing-great-skills` doctrine (leading word, ≤25-word description, completion criterion, no-op test, two-cuts, failure-mode guard) — canonical: the `mattpocock-skills:writing-great-skills` skill. See `docs/skill-authoring-conventions.md` for the kbg-specific additions on top of that (harness-audit check 36's proxy coverage, Named Model footers, Suggested next step footers, AskUserQuestion-escalation criteria) — only load it when actually authoring or editing a skill/command/agent's content.

## Branching model

Single branch: `develop` only. No feature branches. Commit and push direct.

**Computationally enforced** by the `git worktree add -b` block in `gate:bash:irrecoverable` (`PreToolUse:Bash`). Opt-in per repo via the `/.kbg-no-worktree` sentinel — present in the kbg-harness repo, absent from tathep/ECC/scratch repos (which keep their existing `gate:write:worktree-guard` redirect). Detached `review-pr-<N>` worktrees in `$TMPDIR` are explicitly allowlisted so the Phase 2 PR-by-number review path keeps working.

A prior companion gate on the native `WorktreeCreate`/`WorktreeRemove` events (`gate:worktree:develop-only`, `hooks/gates/worktree-create-block.sh`) was removed 2026-07-31 — confirmed against raw Claude Code doc HTML that those events never send `tool_name`/`tool_input` at all, so its deny logic was dead code, and independent of that bug, registering any hook on `WorktreeCreate` replaces Claude Code's default worktree creation and requires the hook to emit the resulting path on success, which this one never did — so it was silently breaking every legitimate `WorktreeCreate`-triggered worktree (`isolation: "worktree"` agents/workflows, `claude --worktree`, background sessions) in every repo running this plugin, not just this one. The Bash-side check above is unaffected — `WorktreeCreate` never fires for Bash-invoked `git worktree add` in the first place. Full writeup: `docs/research/official-docs-audit-2026-07-31.md`.

## Non-obvious gotchas

Grouped by behavior area (not a flat bucket — find the group first, then the line).

### Repo & commit hygiene

- **Hardcoded home paths blocked:** `.sh`/`.py` files must use `$HOME` or `~`, never `/Users/<name>`. The pre-commit gate will reject the commit.
- **Never `rm -rf`:** use `trash` for deletions.
- **Never `--no-verify`** on commits or pushes.
- **Stage by name:** never `git add -A` or `git add .`.

### Plugin lifecycle & install

- **`defaultEnabled: false`:** plugin ships disabled. After install, add `"kbg@kobig": true` to Claude Code `settings.json`, then restart.
- **Cache-invalidation:** same-version edits are no-ops. Always bump both manifests before `claude plugin update`. CLAUDE.md-only edits skip the bump — it's dev-facing repo guidance, not cached plugin content (only `agents/`/`skills/`/`commands/`/`hooks/`/`output-styles/`/`themes/` ship per-version).
- **Re-verifying a same-session edit to any surface:** don't hand a fixture-loop re-verification agent a name-based reference to test an edit that hasn't been bumped/reinstalled yet — not `Skill(<name>)`, not `subagent_type: <name>` for a plugin-scoped Agent, and not the slash-command itself for a Command. `agents/`, `commands/`, and `skills/` all ship inside the same single versioned bundle (`~/.claude/plugins/cache/kobig/kbg/<version>/` — confirmed identical layout across all three, 2026-07-27), so any of those resolutions silently tests stale cached content with no error, regardless of which surface type was edited. Confirmed 2026-07-27 (tech-humanize v0.68.59): one re-verification agent got a false "fix confirmed" this way via `Skill(kbg:tech-humanize)`; instruct the agent to `Read` the repo path directly instead.
- **`BOUNDARY.md` regen:** the script writes to STDOUT, not the file. The `> BOUNDARY.md` redirect is required every time.
- **Output style:** `output-styles/staff-eng.md` is the sole live-response register — self-calibrates terse vs full decision-framing by stakes, not by switching files (the old `senior-eng.md`/`staff-eng.md` two-file split was collapsed 2026-07-02; the internal "Calibrate to stakes" rule replaces the escalation/fallback dance). `force-for-plugin: true` auto-activates it whenever `kbg@kobig` is enabled, overriding the user's own `outputStyle` setting — no `/output-style` selection needed, but it also means you can't run a different style while this plugin is on without disabling it first.

### Session environment quirks

- **Working frames:** `contexts/` holds `dev.md`, `review.md`, `research.md` — loaded by `/frame` to set session posture.
- **`grep` is aliased** to `rtk grep` in this environment. Use `/usr/bin/grep` or `awk` for count/stat operations.

### Skill/agent/command mechanics & routing

- **Skill descriptions load on every Task spawn** (~words×1.3 tokens). Keep descriptions ≤25 words.
- **Thinking models:** default is the triad + `advisor()` inline (METHODOLOGY Rule 1) — `kbg:decide` is on-demand only, for genuinely hard/contested-diagnosis choices (de-scoped 2026-07-02, v0.21.4: 0 real-world invocations vs 55 `advisor()` calls across 182 sessions). The 39 on-demand mental-model files live in `docs/reference/thinking-skills/skills/` (never move to `skills/` — would break fleet count).
- **`disable-model-invocation: true`:** carried by 2 skills currently — `recursive-improve` and `score-decision` (re-check via `for f in skills/*/SKILL.md; do head -20 "$f" | grep -qF 'disable-model-invocation: true' && echo "$f"; done` — frontmatter-scoped, not a bare `grep -rl`, which also matches skills that only *mention* another skill's flag in prose (e.g. `decide`/`orchestrate` cite `score-decision`/`wayfinder`'s flags without carrying one themselves) and would misreport the count; the count drifted hard after v0.46.0 moved 17 flag-carrying matt-origin skills out of this repo, and nobody updated this line). Both are now **CRIT**-guarded against the flag being silently dropped: `recursive-improve/SKILL.md` by check 39, `score-decision/SKILL.md` by check 49 (added 2026-07-23 after a `kbg:plan-reviewer` pass on a skill-improvement batch plan flagged that skill-creator's own description-optimizer rewrites SKILL.md frontmatter, and only recursive-improve had a real gate). Check 30 still only WARNs that a `-reason` field exists on whichever skills currently carry the flag — it's the presence-of-reason check, not the flag-survives-a-rewrite check; the two CRIT checks above are what actually close that gap now.
- **`review_mode` in `ship-merge`:** `review-pr` tags its state write `pr-by-number` (isolated worktree) or `own-branch` (self-review). `ship-merge` caps the Critical-findings score at the fatal-weakness floor on sensitive-path diffs reviewed `own-branch` — an automation-bias guard against trusting a same-session self-review's severity tiering. "Sensitive-path" covers both auth/secret/credential/payment/billing/token AND the harness's own verifier/gate code (`hooks/gates/**`, `hooks/hooks.json`, `skills/harness-audit/scripts/{audit.sh,checks/**}` — the same list `hooks/gates/verifier-protect.sh` protects).
- **`orchestrate` vs `kbg:decide`:** orchestrate decides whether/how to spend effort on an ask (inline/parallel/sequential/drop, which surface receives it) *before* it's understood as a bounded decision; `decide` reasons through a bounded question once you're already committed to answering it. A pile of competing asks routes through `orchestrate` first; a single reversible-choice question goes straight to `decide`.
- **`SKILL.md` frontmatter ≠ `agents/*.md` frontmatter:** the real skill-file field for tool control is `allowed-tools` (pre-approves without asking; there's no hard-restriction field for skills), not `tools:` — that's the `agents/*.md` subagent field (hard-restricts to a fixed set). Confirmed against the official 16-field `skills.md` reference and the shipped CLI binary's own compiled schema key list. `metadata` / `metadata.origin` / `disable-model-invocation-reason` are non-standard-but-harmless kbg conventions — Claude Code tolerates unrecognized frontmatter keys (confirmed via changelog + `plugin.json`'s own documented "unrecognized fields → warning, not error" policy), they just carry zero behavioral effect.
- **`/goal` vs `goal-craft`:** `/goal` is Claude Code's own native completion-condition loop (v2.1.139+, session-scoped, judged each turn by a separate small model that reads only the transcript). kbg never wraps or auto-invokes `/goal` itself — the user always types `/goal` themselves, no exceptions. `skills/goal-craft/SKILL.md` only composes a paste-ready condition string (mandatory one-way-door screen + turn bound) and, as of 2026-07-08, is model-invocable (its `disable-model-invocation` flag was removed on user request) — the model may draft a condition unprompted, but the string is inert until the user pastes it after `/goal`. Auto-dispatch of `/goal` itself (`claude -p "/goal ..."`) is still a deliberate non-goal — it forks a separate headless session and reopens the retired L4/L5 "no model self-start" invariant.

## Recent versions

Quick orientation for the last few releases. For full notes see `CHANGELOG.md`.

Cap this list at 10 individual entries plus one folded range bullet. Keep the last 10 releases as individual entries; fold older ones into version-range bullets (`vX.Y.a–vX.Y.b` or `vX.Y.x`) by theme. Growing past 10 individual entries means folding the oldest into the range bullet instead of adding an 11th — `CHANGELOG.md` is the full record, this section is orientation only.

- **v0.68.126** — Drafted and shipped 3 `output-styles/staff-eng.md` additions after comparing it rule-by-rule against a candidate plugin (`ayghri/i-have-adhd`) on request. Confirmed the plugin is a single-dimension output-shaper (one skill, one opt-in SessionStart hook, no other surfaces) before diffing, so the comparison stayed scoped to voice/format rules rather than functional coverage. Most of its 10 rules were already covered; 3 were real gaps, added as: a `Format` table row requiring `TaskCreate`/`TaskUpdate` to carry multi-turn progress state instead of re-narrating the plan in prose each turn; a `Voice` bullet pairing `advisor()`'s existing "when stuck" trigger with a reply-shape rule (three failed turns on the same fix → name the assumption, stop repeating the fix); and a `Voice` bullet requiring completed work be stated as a concrete resulting capability, not a procedural recap. Declined the plugin's blanket 5-item list cap (conflicts with staff-eng's own stakes-based escape valve and with this file's own dense entries) and its minutes-based time estimates (no direct unit for an agent loop). Full comparison and rationale: `CHANGELOG.md`.
- **v0.68.125** — Ran `/kbg:compliance-audit` (first use) against the session's one formal plan (check 52, v0.68.122) vs. its implementing commit `6cc9bf4`. 10/10 requirements conformed; 3 pre-declared deviations (a wrong plan-text flag, an unplanned `typescript-reviewer.md` edit, an implicit header requirement — all from a pre-implementation `plan-reviewer` pass) independently matched what 3 fresh-context verifiers found on their own. The audit's adversarial-completeness mandate (required for any verifier touching a security/verifier-perimeter file — check 52's script lives under the protected `skills/harness-audit/scripts/checks/**`) found a real bypass: the matcher's bare `not for` alternative matched as a free substring anywhere in a description, false-negativing ordinary prose like "this dashboard is not for production traffic" as conformant. Confirmed 0/30 conformant skills relied on the bare form standalone, removed it outright rather than anchor it — simpler and fully closes the class. Re-verified live: bypass closed, all 3 legitimate forms still match, negative control still fires exactly 1 INFO, full audit still 0 CRIT/0 WARN/5 INFO, gauntlet green. Logged not fixed (Rule 2): uncontracted "Do not use for X" still doesn't match either form — no shipped skill uses that phrasing, latent gap only.
- **v0.68.124** — Closed the item v0.68.123 explicitly deferred: whether ad hoc `/review-fixtures` loops have a proven gap justifying a new persisted regression suite. Dispatched a research agent to sweep `CHANGELOG.md` for `advisor()`'s discriminating test (has a loop ever found a regression in a surface a prior loop already fixed?); the first dispatch spawned its own nested sub-agents and stalled, was reported to the user as failed and relaunched clean — then the original also recovered on its own in the background, giving two independent reports instead of one. They disagreed on 2 of 5 claimed regressions; re-reading this repo's own already-written v0.68.90/v0.68.87 bullets settled it — both say the targeted fixes held and what was found was a new, unrelated bug, not a recurrence. Net: 0 clean "fixed, verified, then broke again" cases; the 2 survivors (`learn`, `summarizer`) are both "never verified live at fix time," a disease `/review-fixtures` already treats when run, not one a rerun suite would catch. The one real gap both reports found independently — `orchestrate` v0.68.58→60, a confirmed finding never applied — is a *tracking* gap, not a *rerun* gap. Verdict: **no proven need, don't build.** Separately, per an explicit user request to re-verify the whole session: re-fetched the ThoughtWorks article v0.68.122 cited and found it mischaracterized — the "deterministic test harnesses" line is real but is the article's cautionary example, not its recommendation (the article's real conclusion is that human judgment stays load-bearing). Fixed the v0.68.122 bullet in place, matching this repo's own v0.68.116-on-v0.68.115 precedent for same-day drill-down corrections; check 52 itself is unaffected. Also re-verified via `git show`/`git cat-file` the two commit hashes v0.68.123's argument depended on (`c452102`, `a518ad1`) plus the `24d7663` recovery anchor — all confirmed accurate.
- **v0.68.123** — Closed the standing item v0.68.122 named but didn't touch: the `204-test critical-hooks suite` + `eval dataset gate` had carried "pending rebuild" across 6+ files for 60+ versions. Verdict after tracing the history: **retire the language, don't rebuild the thing.** Commit `c452102` ("reset: rebuild from scratch", 2026-06-27) was an explicit, owner-authorized deletion, not an accidental regression, and most of what the old suite tested was the L3/L4/L5 autonomy machinery ADR 0006 later retired with a "do NOT re-arm" instruction — rebuilding it would have resurrected exactly what that decision closed. Current coverage already exceeds what "pending rebuild" implied was missing: `run-gauntlet.sh` already wires 8 hook-behavioral test files; `CLAUDE.md`'s own Validation section had only ever named 3. Fixed the doc-accuracy gap and the stale language together across the 5 files that carried it (`CLAUDE.md`, `commands/kbg-help.md`, `commands/ideate/references/provenance.md`, `skills/inventory/scripts/inventory-boundary.sh` — the BOUNDARY.md generator, which also carried a real misattribution bug, blaming the deletion on the later v0.6.0 simplification instead of `c452102`), then went one step further in `git-hooks/pre-commit`: removed ~40 lines of dead `run_eval_layer` machinery that referenced two scripts confirmed absent (`eval/run-eval.py`, `scripts/select-affected-fixtures.py`) rather than leave a no-op existence-check running on every commit forever. Verified: `bash -n` + shellcheck clean on the edited hook; `git diff BOUNDARY.md` shows exactly the 2 intended lines. Deliberately not opened, per `advisor()`: whether a *persisted* regression-eval suite (vs. ad hoc `/review-fixtures` loops) has proven need — separate question, its own evidence bar.
- **v0.68.122** — Added `harness-audit` check 52: a ratchet for the fleet's own de-facto anti-duplication convention (every skill description names a `Don't use for X` boundary). Prompted by mapping a ThoughtWorks article ("Should we still design code for humans?") against this repo's actual state. **Correction (2026-08-01, caught re-verifying this bullet against the primary source):** the article does not recommend "deterministic test harnesses that validate design constraints" — that phrase is real but appears as a cautionary example inside the article's "a spec precise enough to constrain an agent is basically code" argument, not as its answer; the article's actual conclusion is that human judgment stays load-bearing. What was real regardless of the misreading: the gap check 52 closes — nothing detected purpose overlap across the 68 skill/command/agent surfaces, caught so far only by human memory (one confirmed incident, `review-prompt-duplication-pattern`). The first design — a description-similarity detector — was built and rejected empirically: pairwise Jaccard over all 2,278 surface pairs returns deliberate siblings at the top (`backend-patterns`/`frontend-patterns` 0.43, `python-reviewer`/`typescript-reviewer` 0.32), the same false-positive trap that retired check 36's failure-mode proxy 2026-07-16. The data showed a better target instead: 30/30 skills already carry the boundary clause, unenforced — zero findings today, zero false positives by construction. Scoped to skills only (agents/commands are dispatched deliberately, not auto-triggered on description match). A `kbg:plan-reviewer` pass on the approved plan, before any code was written, caught 3 real, reproduced-live gaps: an unstated ordering dependency (the negative-control verification step returns 2 INFOs instead of 1 if run before the `harness-audit` description fix lands — reproduced), a stale hand-authored quote in `agents/typescript-reviewer.md` citing wording this pass deletes from `backend-patterns` (a drift class BOUNDARY.md's auto-regen can't catch, since that file is hand-authored prose, not generated), and an unstated `# 52. ` header requirement load-bearing for `audit.sh`'s split-integrity guard. All 3 fixed pre-implementation. Verified live: negative control (scratch copy, one skill's clause stripped) fires exactly 1 INFO naming that skill; real tree returns 0 findings for check 52, holds 0 CRIT/0 WARN/5 INFO overall; full gauntlet green. (Standing item named here — the `204-test critical-hooks suite` + `eval dataset gate` "pending rebuild" carry — was resolved the next version; see v0.68.123 above.)
- **v0.68.121** — Fixed harness-audit's 5 remaining INFO findings on explicit "fix all remaining" request, with re-verification built in rather than trusting prior sessions' conclusions at face value. `address-review/COMMAND.md` (20,729→20,350) and `ideate/COMMAND.md` (20,846→20,543) got genuine trims (restated sentences, duplicate cost figures) verified to lose no procedural content; both still sit modestly over threshold — accepted residual, same precedent as prior passes. `orchestrate/SKILL.md` (32,316) and `review-pr/SKILL.md` (42,109) — already optimized once in v0.68.115 — got a fresh read-through plus a phrase-repetition grep specifically to check whether that pass's "further cuts would lose real content" conclusion still held, rather than assuming it did: it held, no redundant prose found in either file (review-pr's one apparent repeat — the submit-gate "never asked twice" rule at 3 points — is deliberate reinforcement at each real decision point where missing it double-posts to a live PR). `harness-audit`: 0 CRIT, 0 WARN, 5 INFO (unchanged count, smaller numbers where files moved). Also closed iteration-3 of an address-review-workspace fixture-testing pass — three iterations found no discriminator between address-review's doctrine and generic engineering judgment on Phase 5 bookkeeping; the doctrine file itself was not edited by that pass. Full write-up: `address-review-workspace/iteration-3/feedback.json`.
- **v0.68.120** — Full-project official-docs accuracy audit (workflow-driven, 218 claims across 13 domains checked against live primary sources): 37 double-confirmed inaccuracies, 17 unverifiable, 0 reversed on recheck. Full record: `docs/research/official-docs-audit-2026-07-31.md`. Critical finding: the `WorktreeCreate`/`WorktreeRemove` gate (`gate:worktree:develop-only`) had been dead code as a deny mechanism since v0.29.0 — it read `tool_name`/`tool_input.branch`, fields those events never send (confirmed against raw doc HTML after a WebFetch summarizer fabricated a fake schema mid-investigation, caught and discarded) — and independent of that bug, registering any hook on `WorktreeCreate` replaces Claude Code's default worktree creation and requires the hook to emit the resulting path, which this one never did, so it was silently breaking every legitimate `WorktreeCreate`-triggered worktree (`isolation: "worktree"`, `claude --worktree`, background sessions) in every repo running this plugin — confirmed already happened live via a 2026-07-28 memory record. Fixed by deregistering both hooks.json entries and deleting the dead gate + its 34-test suite (which validated against the same invented payload shape, self-consistent but reality-disconnected); the develop-only doctrine's real enforcement point was always the `git worktree add -b` block in `gate:bash:irrecoverable`, unaffected. `hook-lifecycle-contracts.md` rewritten to match (event/hook counts, `permissionDecision` enum, a stale `path-hardcode.sh` row, a false "no PostToolUse hooks" claim). Also fixed: `cost-tracker.sh`'s stale Haiku/Opus/Sonnet pricing (verified live, unit + end-to-end tested), a broken `gh pr checks` field name in `skills/pr/SKILL.md`, stale MCP-tool-cost/context-window assumptions in `context-budget`/`env-vars.md`, and ~15 domain-skill content drifts (Next.js middleware syntax, Zustand's curried-create pattern, Drizzle's array-form index syntax, MariaDB variable naming, TypeScript 7.0 GA status, cost-ratio/cache-token claims, a Node `worker_threads` vs. sync-I/O miscategorization, and a misattributed README citation). `security-auditor`'s OWASP 2021→2025 edition move left as a stated judgment call, not auto-migrated. One meta-incident during the fix: an apostrophe added to a comment inside `irrecoverable.sh`'s `python3 -c '...'` single-quoted heredoc broke bash quoting and locked out every `PreToolUse:Bash` call for a few tool calls — exactly the `gate-python-c-quoting-lockout` failure mode from memory; recovered via `Edit` (a different tool, different gate) rather than Bash, per the documented recovery precedent.
- **v0.68.119** — Rewrote all 3 `contexts/` files (`dev.md`/`review.md`/`research.md`), on explicit request, after a drill-down found they were stale two ways at once: commit `12d219f` (2026-06-28) had swapped kbg-custom versions for generic `everything-claude-code` stock templates citing zero kbg doctrine (no Rule numbers, no fleet routing), but the pre-swap kbg-custom versions weren't a valid revert target either — they cited "Two-Axis Triage" and a "ponytail ladder" as named concepts that no longer exist in current doctrine, plus a dead agent (`pr-test-analyzer`). Rewrote fresh: each file now points at Rule numbers (3/4/1) instead of reprinting them (doctrine-bootstrap already injects the full text every session), routes to the current agent fleet, and `review.md`'s triage language now matches `review-pr` Phase 5's actual Critical/Important/Minor scheme. `research.md`'s qmd/context7-first guidance is phrased generically rather than hardcoding this operator's own collection names, since `contexts/` ships to every plugin installer (confirmed via a live plugin-cache listing — it's a full-repo mirror, not scoped to CLAUDE.md's "Adding or removing a surface" list). `BOUNDARY.md` untouched by design — confirmed `contexts/` isn't part of the inventory tooling's scan surface before skipping the regen.
- **v0.68.118** — Follow-up to v0.68.117's check-51 finding, on explicit request: split `ideate/COMMAND.md` (23,372 chars, over the 20K threshold) the same way. It was already directory-form (had `references/frames.md`), so this added a second reference file (`references/provenance.md`) rather than converting the surface's shape — consolidated 4 scattered upstream "Source:" citations, the 2-wave fan-out WARNING's audit-history paragraph (kept the load-bearing WARNING itself inline, moved only the historical justification), and the full Cross-references section. Every extraction verified line-range-exact via `diff` against `git show HEAD`; the only differences were 3 deliberate cross-reference retargets (anchor links don't resolve across files) — no content lost. 23,372→20,846 chars; same accepted residual-overage outcome as `address-review.md` (v0.68.117) and orchestrate/review-pr (v0.68.115) — forcing the last ~800 chars out risked cutting real content for a cosmetic threshold clear. `harness-audit`: 0 CRIT, 0 WARN, 5 INFO (both size-outlier INFOs shrank, neither cleared). Full gauntlet green.
- **v0.68.117** — Requested drill-down on "which commands should convert to skills" — answer: none. The 2026-07-07 conversion criterion (bundled scripts/reference files/`paths:`) is stale: `docs/command-authoring-conventions.md` (2026-07-20) already established directory-form commands (`COMMAND.md` + `references/`, used by `ship`/`ideate`) give bundled reference files without a skill conversion, and a repo-wide grep confirmed 0/30 skills use `paths:` scoping — the one remaining theoretical differentiator has no live precedent. Shipped the 2 real findings the analysis surfaced instead: added `harness-audit` check 51, the command-side counterpart to check 42's 20K-char SKILL.md size-outlier check (no prior check covered commands at all) — it immediately caught 2 real outliers, `address-review.md` (23,556 chars, found by the manual survey) and `ideate/COMMAND.md` (23,372 chars, missed by the manual survey since it only scanned flat files); bumped `audit.sh`'s check-fragment integrity guard 50→51. Split `address-review.md` into directory-form (`address-review/COMMAND.md` + `references/`), matching `ship`/`ideate` — extracted the Phase 1 GraphQL query and the Integration Notes section, each verified byte-for-byte against the original via `diff` before deleting the flat file. 23,556→20,729 chars; left the residual ~700-char overage alone rather than force an artificial trim, per the v0.68.115 precedent. `harness-audit`: 0 CRIT, 0 WARN, 5 INFO (up from 3 — both new INFOs are real findings, not regressions; `ideate/COMMAND.md`'s own split is left open). Full gauntlet green.
- Older releases (v0.68.27–v0.68.116): a drill-down full audit of v0.68.115's diff (v0.68.116) that found and fixed 4 real defects — a safety-relevant content-loss in `orchestrate/SKILL.md` (a dropped two-part reopen gate, restored inline), a false `review-pr/SKILL.md` claim that a non-zero exit means the state-file write didn't happen, a docs claim that a section was *cut* when it was only compressed, and a stale `security-auditor` routing line contradicting the rule next to it; a token-optimizer pass on orchestrate/review-pr (v0.68.115 — orchestrate 38,418→32,130 chars, review-pr 49,258→41,877 chars, extracting the `/ship-merge` state-file writer into `scripts/write-review-state.sh`; both still exceed the 20k advisory threshold by design, forcing lower would cut safety-critical content); a token-optimizer pass on backend-patterns/frontend-patterns (v0.68.114 — split both into a lean trigger+gotchas SKILL.md plus a reference.md holding every code example, 20,954→9,324 and 24,988→9,619 chars, every prose insight kept and verified by phrase-match) closing the candidate the entry below it had left open; a harness-audit check-36 false-positive fix (v0.68.113 — the leading-word vocabulary had `backend` but was missing its exact structural sibling `frontend`); closed harness-audit's last remaining WARN (v0.68.112, check 47 description budget) by making it read the actually-configured budget dynamically instead of a hardcoded default; first `harness-audit` run since the `llm-wiki` wiring landed (v0.68.111) fixed check 37 (dead-script-pointer, 4 false positives against wiki-scan/wiki-ingest) and 2 smaller `typescript-patterns` findings; a `wiki-scan`/`stats.sh` disagreement caveat added (v0.68.110, `stats.sh`'s "Orphans: N" line can disagree with `lint-scan.sh`'s canonical Summary since `stats.sh` never received an `index.md`-scanning fix `lint-scan.sh` got in commit `3d9f2ea` — the underlying `stats.sh` bug stays unfixed by design, wrap-not-reimplement); a proposed second-brain "folder template" idea mapped against this project's actual `llm-wiki` wiring (v0.68.109, per Rule 2 — most categories already existed in leaner form, Triggers/Graph Shapes stayed rejected, nothing new built) plus one real fix surfaced along the way (`wiki-scan/SKILL.md`'s `unsourced` counter gloss had the citation direction backwards); a second `/kbg:review-fixtures` loop (v0.68.108, `wiki-ingest.md`, the write-capable sibling of v0.68.107's `wiki-scan` fix) built 2 evals with 4 isolated per-arm clones and fixed 3 real gaps (a topic-mismatch edge case, a missing skip-parenthetical asymmetry, a hotcache gotcha that inferred instead of requiring quoted output — full reconciliation in `wiki-ingest-workspace/iteration-1/feedback.json`); a first-ever loop for `wiki-scan` (v0.68.107) found and fixed a quadruple-confirmed unexported-`VAULT` bug (silently fell back to the default vault path regardless of `KBG_WIKI_VAULT`) plus 2 smaller gaps (an undocumented unsafe `lint-wiki.sh`, a missing broken-wikilink check — full reconciliation in `wiki-scan-workspace/iteration-1/feedback.json`); wired the operator's personal `llm-wiki` knowledge vault into kbg for cross-project reach (v0.68.106) — `/kbg:wiki-ingest` (a `disable-model-invocation: true` command wrapping the vault's `ingest.sh`) and `kbg:wiki-scan` (read-only, wraps `lint-scan.sh`/`stats.sh`), plus a global `~/.claude/CLAUDE.md` rule for the always-on layer; a `kbg:plan-reviewer` pass on the plan found and fixed 5 real issues before any code was written. An `advisor()` pass on v0.68.104's just-shipped fix (v0.68.105) caught 3 guardrail-tightening gaps in `agents/task-prep-checker.md` — a `done_when_shape` credited `repro` from surface features after the premise check had already failed (fixed to require `missing`), a re-verification dispatch prompt that confounded its own schema-clean result with a redundant restated instruction (re-ran with it removed, held clean regardless), and an unpinned `<done-when>` placeholder instead of the bare field name; all 3 documentation-only, no behavior change; First-ever `/kbg:review-fixtures task-prep-checker` loop (v0.68.104) — 2 reviewers converged on 2 real, double-confirmed gaps: Phase 4's `done_when_shape` classification credited a `repro` shape from surface features alone without cross-checking the check's premise, and 2 of 3 live runs violated the agent's own Output Format under complex judgment calls; fixed both, re-verified live; First `/kbg:review-fixtures claude-md-health` loop (v0.68.103) — 3 fixtures built as real isolated synthetic git repos with controlled commit dates, exercising the skill's actual `git log`/`grep` mechanics rather than prose-only cases; all 3 core mechanics held cleanly, one real grep-verified gap fixed (the verdict-column instruction was a loose example instead of a pinned literal string, producing inconsistent formatting across dry runs); `/kbg:review-fixtures task-prep` (v0.68.102), deliberately scoped past a standing "don't re-audit without new evidence" memory since `v0.66.0` added a genuinely new capability since that critique — Step 3.5's non-Jira `requirement-analyst` dispatch path. Both reviewers converged on a real regression there (a SendGrid v2→v3 migration misread as a covered routine bump, skipping the dispatch that would've caught the real risk), fixed by clarifying the exclusion; a second, single-sourced fix closed a gap where `riskiest_assumption` competed anonymously in the generic open-questions list; `/kbg:review-fixtures learn` drill-down (v0.68.101) verified v0.68.47's 4 findings actually landed, then re-ran all 3 fixtures fresh and found a real regression — the "tool/platform default already enforces it" dedupe-exclusion clause fired too broadly on advisory prose and dropped a negative-discipline candidate that should have survived; narrowed to deterministic/fail-closed, project-scoped defaults, re-verified live; `scripts/workflows/deep-research.js` committed (v0.68.100), the canonical deep-research Workflow script consolidated from 8 ad-hoc inline runs that had each re-authored the same ~23KB script from scratch — Scope → pipeline(Search → dedup → Fetch) → barrier → 3-vote adversarial Verify → Synthesize, hardcoded fan-out caps instead of leaving size to the model; an mtime-based staleness signal added to `memory-lint` (v0.68.99), the one piece worth porting from a direct comparison against ECC's `continuous-learning-v2` instinct system — ECC's decay formula (−0.02/week unobserved) mirrored as a check flagging entries untouched past `--stale-days` (default 90), advisory-only, not counted toward the exit code; a known-gap callout for the shared "70" pass-threshold constant filed in-file per user request (v0.68.98, `commands/ship-merge.md`, documentation only, no behavior change — see `CHANGELOG.md` for the full entry); a `/kbg:compliance-audit` first-run pass (v0.68.97) on `ship-merge.md`'s automation-bias guard found and fixed two further bypasses in the just-shipped weight-≥15 invariant (generalized to the live ratio `D≥(7/3)×C`, then closed a second bypass via an explicit dual-recompute instruction), with a fourth, different-mechanism finding (the shared "70" pass-threshold constant) logged as an out-of-family known-gap per Rule 2; a review depth/level system for `kbg:review-pr` was investigated and rejected (Rule 2, v0.68.96) — the target population (single-file, non-test diffs) is 5.7% and already runs at the cost floor — but investigating *why* the need felt unmet surfaced a real bug instead: PR #2603 had invoked the Rule-2 trivial-diff economy to justify dropping `code-reviewer` for `typescript-reviewer` alone, a Phase 3 violation by substitution; fixed the substitution path plus a previously-unstated weight-≥15 invariant in `ship-merge.md`'s automation-bias guard; fixed a dangling cross-reference in `commands/review-fixtures.md` (v0.68.95, caught by a drill-down audit of the v0.68.94 fix itself, not a fixture loop) — Step 6 pointed back to "Step 0 above" but the file had no `### 0.` heading, only an unnumbered intro paragraph; fixed by promoting that paragraph into a real `### 0.` step; fixed `/review-fixtures` itself (v0.68.94) after surveying open gaps from everything learned running it — Step 6 now requires reviewer-dispatch eval prompts be quoted **verbatim**, never paraphrased (the v0.68.93 prompt-compression finding), and a new Step 3.5 reads an existing `feedback.json` before dispatching fresh reviewers instead of silently re-doing or skipping the work; also institutionalized 3 fixture-construction lessons (answer-key contamination, bench-script import-not-duplicate, trap file-locality) into `docs/reference/skill-fixture-review-prompt-template.md`; a `/review-fixtures`-style loop for `skills/score-decision/SKILL.md` (v0.68.93, first-ever for this target) found a headline "evidence-laundering" finding across both reviewers that did NOT survive verification (traced to a shared blind spot in the compressed `dispatch-prompts.md` paraphrase, not a real doc gap) — but two real, double-confirmed findings did survive, closing a Step 3 secondhand-evidence ambiguity and a Ranking-format auditability gap for a disqualified option with the highest raw weighted sum; First-ever `/review-fixtures`-style loop for `skills/recursive-improve/SKILL.md` (v0.68.92, no prior workspace existed) — real scripts/mutations meant `advisor()` was consulted on fixture safety pre-dispatch; one eval-1 baseline still ran real scripts against no explicit "don't touch real files" instruction and edited the real, already-shipped `skills/decide/SKILL.md` on disk, caught and reverted immediately, eval rebuilt with fictional paths; 2 independent reviewers found and fixed the strongest, double-confirmed issue — a fabricated "executed" witness-diff comparison that never ran — plus a headless-wait wording fix and a template gap for the unanswered-gate branch; a `/review-fixtures`-style loop for `skills/decide/SKILL.md` (v0.68.91, first-ever for this target) found and fixed a rungs-1–2 proportionality contradiction between the v0.68.32 fix and the `decide` completion criterion/Guardrails, a bundled-multi-row gap, and a bias-guard-ordering issue; a deeper re-verification pass on `agents/code-architect.md` (v0.68.90, blind-spot-hunter v0.68.87 precedent) re-ran `with_agent` fresh against the post-v0.68.86-fix doc for all 3 original evals — confirmed all 3 v0.68.86 fixes closed against actual shipped code, and fixed a regression neither original fix targeted (a domain→infra anti-pattern via an unqualified "closest analog is ground truth" bullet, fixed with an explicit precedence exception); a `/review-fixtures ship` pass (v0.68.89) on `commands/ship/COMMAND.md` found Phase 8 has no PR fallback for the ordinary first-push Path B case (its only action, "Invoke `/ship-merge`," hard-requires a PR) plus a GREEN-branch "no PR yet" note pointed at the wrong phase — fixed both, plus a tool-calling-vs-string-handoff wording nit; applied both target-attributable fixes iteration 3 of `build-error-resolver-workspace` had left pending (v0.68.88) — reworded the Recovery Strategies "Type mismatch" row to name the actual decision criterion (correct whichever declaration doesn't match the surrounding code's stated invariant, not "whichever type is narrower," which misdirected toward a masking trap), and added a Step 5 report-template requirement to note rejected alternatives that would also have built clean; also closed a separate iteration-4 gap via a new fixture where the correct fix unavoidably surfaces a second real error — no doctrine gap there, just a narrowed measurement claim; a session closing a gap in its own review-fixtures practice (v0.68.87) found `blind-spot-hunter`'s v0.68.43 fix had never been checked by an independent 2-agent pass or at the output level — re-ran 4 `with_agent` dry-runs fresh, confirmed all 3 targeted gaps closed, and fixed one real regression neither original review anticipated (a fresh run executed a mutation-test script in scratch space via a relocate-outside-the-repo loophole the doc's "must not run these" rule didn't cover); a `/review-fixtures agents/code-architect.md` loop (v0.68.86) found 3 doctrine gaps via 3 fixtures targeting layer-direction detection, premature-abstraction bait, and a load-bearing ambiguous requirement — fixed via a test-suite check, a code slot in the Output Format, and guidance for surfacing ambiguous requirements prominently; a first fixture-driven loop for `agents/backend-architect.md` (v0.68.85, no prior workspace existed) built 3 realistic 2-file fixtures and ran with-agent vs. baseline (`general-purpose`, no doctrine) — baseline independently caught a concurrency bug (agent 2 spun up a live Postgres container and proved with_agent's proposed atomic-claim SQL doesn't close the race, guard column ≠ mutated column) and a client-controlled-pricing bug that with_agent's own checklist missed on the same line; fixed via a worked-example callout on the guard-column requirement and a new "Source of truth" checklist row + matching Anti-Patterns row; a `summarizer` agent loop (v0.68.84, first use since v0.68.83's dispatch-prompts fix) found `iteration-1`'s fixtures predated v0.68.41's fix, so `advisor()` redirected the dispatch to inline the current doc for both agents and check whether v0.68.41's own Prose/Bullets/Table fork left a contradiction the 3 prior solo convergence rounds never checked for — both agents independently confirmed it had (Table's criterion structurally matched the Anti-Patterns causal-chain example without excluding it, self-contradictory OR/AND wording, a citation gap in the multi-throughline Output Format), all fixed and self-verified via whole-file re-read; a dispatch-prompts persistence fix (v0.68.83) for a gap confirmed twice — `/review-fixtures` composed dispatch prompts live in-conversation and never saved them, so the literal text reviewers received became unrecoverable once the session compacted; fixed in `commands/review-fixtures.md` Step 6 by writing both composed prompts verbatim to `<iteration-path>/dispatch-prompts.md` before dispatching (this fix's own file-persistence discipline is what later made the v0.68.93 prompt-fidelity correction possible to verify); a `ship-merge-workspace` pass (v0.68.82) closing 2 deferred fixture gaps (CI-partial-pending, missing-review-state) with 3 grep-verified doc fixes; a `cost-tracker.sh` fix (v0.68.81) that stopped attributing 100% of a mixed-model session's cost to whichever model was active at the last stop — grouped assistant turns by `.message.model` instead of tagging one whole-session sum, verified against a real 3-model transcript and regression-gated byte-identical against production data; a chased-and-initially-wrong multi-model cost-attribution bug (v0.68.80 — a plausible-looking "fix" would have double/triple-counted tokens, caught by reading the actual writer hook and a telescoping-sum identity proving the original total was correct all along; the real, narrower bug documented as a known limitation instead); a real UTC-vs-local timezone bug in `cost-report.md`'s "today"/"yesterday" buckets (v0.68.79, reproduced live — unfixed report showed "today: $972.84" when the real local-today total was $376), picked as a `/review-fixtures` target because it has real local production data (`costs.jsonl`) to mine directly; a fleet-count drift fix (v0.68.78, `plugin.json`/`marketplace.json`/`README.md` all claimed "18 commands" against a real 16 — same class of drift v0.68.72 fixed for the agent count); a `review-pr`-workspace pass (v0.68.77) mined 105 real production state files directly instead of dispatching fixtures first, finding the `rehunt` field held 15+ distinct shapes against 4 documented canonical values (20/105 missing it entirely) — fixed by treating a missing-or-non-canonical value the same as explicit `"incomplete"` in `ship-merge.md`'s Incomplete-review guard, closing the gap for both the session-that-wrote-it and the sessions-that-never-populated-it populations; a sustained `/review-fixtures` improve+optimize sweep across the fleet — build-error-resolver, refactor-cleaner, security-reviewer, backend-patterns/frontend-patterns builds and re-reviews, orchestrate, tech-humanize, task-prep, decide, ship-merge, address-review, silent-failure-hunter, requirement-analyst, typescript-reviewer, summarizer, blind-spot-hunter, code-implementer (×2), code-reviewer, typescript-patterns, python-reviewer, performance-optimizer, plan-reviewer (2nd reconciliation pass, v0.68.75, surfaced the read-first gap fixed by v0.68.83 above), and nextjs-reviewer (a factual/scoping-bug pass in v0.68.73 — 4 fixes, including a `proxy.ts` scoping gap that would've silently skipped the file its own Middleware guidance covers — then a first full with-baseline 2-reviewer eval loop in v0.68.74, which found the specialist's real edge was severity calibration, not raw bug-catching, and fixed a backwards `.bind()` Server Action encryption claim), plus the `/review-fixtures` command's own workspace-schema generalization (v0.68.62/.63) and self-check (v0.68.61). Each loop found and fixed 1-5 real bugs, verified against fixtures or live tools (Postgres, Node, `tsc --strict`). Also: `flutter-reviewer` removal (v0.68.72) and two Tathep framework-pattern removal batches (`adonisjs-patterns`/`dart-flutter-patterns`/`effect-ts-patterns`/`fastapi-patterns`/`hono-patterns`/`langchain-langgraph-patterns`/`tauri-v2-patterns` in v0.68.51, then `codebase-onboarding` in v0.68.52). See `CHANGELOG.md` for the full per-release record.
