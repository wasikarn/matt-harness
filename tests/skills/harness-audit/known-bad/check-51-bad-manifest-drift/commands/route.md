# Fixture routing command — clean

References only the model-invocable `mattpocock-skills:foo`; the fixture's violations live
in the fake cache's plugin.json format and the ledger's duplicate row, not here.
