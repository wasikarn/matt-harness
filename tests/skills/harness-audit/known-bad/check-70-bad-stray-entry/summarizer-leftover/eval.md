Stray eval leftover left behind after its agent was deleted. Check 70 must WARN on the
`summarizer-leftover/` directory that contains this file.

Named without a `-workspace` suffix on purpose: `.gitignore`'s `*-workspace/` pattern is
unanchored and would swallow this fixture whole, so it would never reach git.
