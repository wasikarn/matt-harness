# Fixture CLAUDE.md — check 51 bad

Sub-check A violation: follow the `mattpocock-skills:ghost` doctrine (renamed upstream,
this ref is dead).

Sub-check B violation: `mattpocock-skills:foo` is `disable-model-invocation: true` so only
the user can start it (wrong — foo carries no flag in the fixture cache).
