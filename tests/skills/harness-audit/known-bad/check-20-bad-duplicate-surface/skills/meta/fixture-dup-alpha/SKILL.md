---
name: fixture-widget-tool
description: "Provisions internal widget environments end-to-end for engineering teams who need a fast repeatable setup without manual configuration steps."
---

# fixture-dup-alpha

Self-test fixture (skill half) for check 20.5 — shares `name:
fixture-widget-tool` with the agent half in `../../agents/fixture-dup-agent.md`,
and the two descriptions are near-identical (differ by one word), so the
duplicate-surface detector must WARN.
