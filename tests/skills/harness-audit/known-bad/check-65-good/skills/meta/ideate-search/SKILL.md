---
name: ideate-search
description: "self-test fixture — a real, already-guarded carrier, so check 65 stays silent"
disable-model-invocation: true
disable-model-invocation-reason: test fixture only
---

# Ideate Search

Fixture body. This fixture's tree also scaffolds a matching
skills/meta/harness-audit/scripts/checks/58-*.sh stand-in carrying the same
`_f=` line the real check 58 uses — check 65 reads that file's text only (it
never sources it), so the scaffold is what lets this fixture prove the
already-guarded, silent-check-65 case.
