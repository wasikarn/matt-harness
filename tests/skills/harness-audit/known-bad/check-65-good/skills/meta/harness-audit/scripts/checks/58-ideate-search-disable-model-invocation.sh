#!/usr/bin/env bash
# 58. fixture stand-in for the real check-58 guard — only the _f= line matters
# to check 65's own detection logic (a grep over this text, never sourced).
_f="$CLAUDE_DIR/skills/meta/ideate-search/SKILL.md"
