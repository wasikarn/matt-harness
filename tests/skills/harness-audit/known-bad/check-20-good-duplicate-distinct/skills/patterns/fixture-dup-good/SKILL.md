---
name: fixture-shared-name
description: "Handles data export to CSV format for reporting teams needing scheduled batch downloads."
---

# fixture-dup-good

Self-test fixture (skill half) for check 20.5 — shares `name:
fixture-shared-name` with the agent half in
`../../agents/fixture-dup-good-agent.md`, but the descriptions cover
genuinely different topics (low similarity), so the duplicate-surface
detector must stay silent — a legitimate skill+agent name twin, not a
content twin.
