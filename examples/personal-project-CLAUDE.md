# CLAUDE.md — <PROJECT_NAME> (personal / solo)

> Starter. Copy to a personal repo as its `CLAUDE.md`, fill every `<PLACEHOLDER>`, delete this note.
> Lean by default — a solo repo doesn't need team ceremony.

## What this is

<PROJECT_NAME> — <one line>. Stack: `<LANGUAGE/FRAMEWORK + key deps>`.

## Conventions

- **Branching:** `<e.g. single-branch main, commit + push direct — no feature branches>`.
- **Commit/push:** `<e.g. only when I ask>` — state it so the agent doesn't auto-push.
- **Simplicity first (YAGNI):** no speculative config, no abstraction with one caller, shortest working diff.

## Build / test / run

- Setup: `<SETUP_CMD>`
- Test: `<TEST_CMD>` — run before committing non-trivial logic
- Run: `<RUN_CMD>`

## Notes

- `<ANYTHING NON-OBVIOUS A FRESH SESSION MUST KNOW — env vars, a load-bearing file, a gotcha>`
