{
  "skill_name": "semantic-code",
  "last_reviewed_reason": "not reviewed in 2026-06-11 epic; deferred to quarterly cadence in docs/harness-decay-cadence.md (first sweep 2026-09)",
  "evals": [
    {
      "id": 1,
      "prompt": "What functions and classes changed in commit 2b35bb0? Give me the entity names and change types (added/modified/deleted).",
      "expected_output": "Uses `sem diff --commit 2b35bb0 --format json` to get entity-level changes, then reports the function/class names and their change types.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "I need focused context on the `is_secret_path` function in `claude/hooks/secret-read-guard.sh`. Include what it calls and what calls it, but keep it under 4000 tokens.",
      "expected_output": "Uses `sem context is_secret_path --file claude/hooks/secret-read-guard.sh --budget 4000 --json` to get token-budgeted context including dependencies and dependents.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Show me the raw line-level diff patch for commit a43b59f.",
      "expected_output": "Uses plain `git show a43b59f` or `git diff a43b59f^ a43b59f` \u2014 does NOT use `sem diff` because the user explicitly asked for raw line-level diff.",
      "files": []
    }
  ]
}
