{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "idea-audit attacker result",
  "type": "object",
  "required": ["pass", "findings", "checked"],
  "additionalProperties": false,
  "properties": {
    "pass": {
      "type": "boolean"
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["summary", "evidence"],
        "additionalProperties": false,
        "properties": {
          "summary": {
            "type": "string",
            "pattern": "\\S"
          },
          "evidence": {
            "type": "string",
            "pattern": "\\S"
          }
        }
      }
    },
    "checked": {
      "type": "array",
      "minItems": 1,
      "description": "At least one claim actually re-checked against primary evidence, required even on a clean pass with an empty findings[] — closes the vacuous-accept case where pass:true/findings:[] is schema-valid but shows no independent verification work.",
      "items": {
        "type": "object",
        "required": ["claim", "evidence"],
        "additionalProperties": false,
        "properties": {
          "claim": {
            "type": "string",
            "pattern": "\\S"
          },
          "evidence": {
            "type": "string",
            "pattern": "\\S"
          }
        }
      }
    }
  }
}
