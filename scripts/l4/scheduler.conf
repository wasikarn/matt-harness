{
  "_doc": "scripts/l4/scheduler.conf — the SINGLE sanctioned self-launch config (ADR 0004, design §5 F3 + §8). Caged via scripts/l4/** — the loop can never edit its own launch cadence. Kill-switch = delete this file (or unset KBG_AUTONOMY); the launcher refuses to run without it. Read by scripts/l4/launch.sh (Slice 3, issue #31); Slice 0 (#19) only DEFINES + CAGES the path — nothing self-launches yet. interval_seconds = launchd StartInterval cadence (throttled by R4 cumulative caps, issue #23). max_runs_per_window / window_seconds = R4 cross-run ceiling persisted separately in scripts/l4/.window-state.json (caged).",
  "interval_seconds": 3600,
  "max_runs_per_window": 6,
  "window_seconds": 21600
}